import { useState, useEffect, useMemo, useCallback, useRef } from "react";
import * as d3 from "d3";

// ═══════════════════════════════════════════════════════════════════════
// JOIST LIBRARY DATA — [span_ft, total_load_plf, live_load_l360_plf]
// ═══════════════════════════════════════════════════════════════════════
const JOIST_DB = {"10K1":[[10,550,550],[11,550,542],[12,550,455],[13,479,363],[14,412,289],[15,358,234],[16,313,192],[17,277,159],[18,246,134],[19,221,113],[20,199,97]],"12K1":[[12,550,550],[13,550,510],[14,500,425],[15,434,344],[16,380,282],[17,336,234],[18,299,197],[19,268,167],[20,241,142],[21,218,123],[22,199,106],[23,181,93],[24,166,81]],"12K3":[[12,550,550],[13,550,510],[14,550,463],[15,543,428],[16,476,351],[17,420,291],[18,374,245],[19,335,207],[20,302,177],[21,273,153],[22,249,132],[23,227,116],[24,208,101]],"12K5":[[12,550,550],[13,550,510],[14,550,463],[15,550,434],[16,550,396],[17,550,366],[18,507,317],[19,454,269],[20,409,230],[21,370,198],[22,337,172],[23,308,150],[24,282,132]],"14K1":[[14,550,550],[15,511,475],[16,448,390],[17,395,324],[18,352,272],[19,315,230],[20,284,197],[21,257,170],[22,234,147],[23,214,128],[24,196,113],[25,180,100],[26,166,88],[27,154,79],[28,143,70]],"14K3":[[14,550,550],[15,550,507],[16,550,467],[17,495,404],[18,441,339],[19,395,287],[20,356,246],[21,322,212],[22,293,184],[23,268,160],[24,245,141],[25,226,124],[26,209,110],[27,193,98],[28,180,88]],"14K4":[[14,550,550],[15,550,507],[16,550,467],[17,550,443],[18,530,397],[19,475,336],[20,428,287],[21,388,248],[22,353,215],[23,322,188],[24,295,165],[25,272,145],[26,251,129],[27,233,115],[28,216,103]],"14K6":[[14,550,550],[15,550,507],[16,550,467],[17,550,443],[18,550,408],[19,550,383],[20,525,347],[21,475,299],[22,432,259],[23,395,226],[24,362,199],[25,334,175],[26,308,156],[27,285,139],[28,265,124]],"16K2":[[16,550,550],[17,512,488],[18,456,409],[19,408,347],[20,368,297],[21,333,255],[22,303,222],[23,277,194],[24,254,170],[25,234,150],[26,216,133],[27,200,119],[28,186,106],[29,173,95],[30,161,86],[31,151,78],[32,142,71]],"16K3":[[16,550,550],[17,550,526],[18,508,456],[19,455,386],[20,410,330],[21,371,285],[22,337,247],[23,308,216],[24,283,189],[25,260,167],[26,240,148],[27,223,132],[28,207,118],[29,193,106],[30,180,96],[31,168,87],[32,158,79]],"16K4":[[16,550,550],[17,550,526],[18,550,490],[19,547,452],[20,493,386],[21,447,333],[22,406,289],[23,371,252],[24,340,221],[25,313,195],[26,289,173],[27,268,155],[28,249,138],[29,232,124],[30,216,112],[31,203,101],[32,190,92]],"16K5":[[16,550,550],[17,550,526],[18,550,490],[19,550,455],[20,550,426],[21,503,373],[22,458,323],[23,418,282],[24,384,248],[25,353,219],[26,326,194],[27,302,173],[28,281,155],[29,261,139],[30,244,126],[31,228,114],[32,214,103]],"16K6":[[16,550,550],[17,550,526],[18,550,490],[19,550,455],[20,550,426],[21,548,405],[22,498,351],[23,455,307],[24,418,269],[25,384,238],[26,355,211],[27,329,188],[28,306,168],[29,285,151],[30,266,137],[31,249,124],[32,233,112]],"16K7":[[16,550,550],[17,550,526],[18,550,490],[19,550,455],[20,550,426],[21,550,406],[22,550,385],[23,507,339],[24,465,298],[25,428,263],[26,395,233],[27,366,208],[28,340,186],[29,317,167],[30,296,151],[31,277,137],[32,259,124]],"16K9":[[16,550,550],[17,550,526],[18,550,490],[19,550,455],[20,550,426],[21,550,406],[22,550,385],[23,550,363],[24,550,346],[25,514,311],[26,474,276],[27,439,246],[28,408,220],[29,380,198],[30,355,178],[31,332,161],[32,311,147]],"18K3":[[18,550,550],[19,514,494],[20,463,423],[21,420,364],[22,382,316],[23,349,276],[24,320,242],[25,294,214],[26,272,190],[27,252,169],[28,234,151],[29,218,136],[30,203,123],[31,190,111],[32,178,101],[33,168,92],[34,158,84],[35,149,77],[36,141,70]],"18K4":[[18,550,550],[19,550,523],[20,550,490],[21,506,426],[22,460,370],[23,420,323],[24,385,284],[25,355,250],[26,328,222],[27,303,198],[28,282,177],[29,263,159],[30,245,144],[31,229,130],[32,215,118],[33,202,108],[34,190,98],[35,179,90],[36,169,82]],"18K5":[[18,550,550],[19,550,523],[20,550,490],[21,550,460],[22,518,414],[23,473,362],[24,434,318],[25,400,281],[26,369,249],[27,342,222],[28,318,199],[29,296,179],[30,276,161],[31,258,146],[32,242,132],[33,228,121],[34,214,110],[35,202,101],[36,191,92]],"18K6":[[18,550,550],[19,550,523],[20,550,490],[21,550,460],[22,550,438],[23,516,393],[24,473,345],[25,435,305],[26,402,271],[27,372,241],[28,346,216],[29,322,194],[30,301,175],[31,281,158],[32,264,144],[33,248,131],[34,233,120],[35,220,110],[36,208,101]],"18K7":[[18,550,550],[19,550,523],[20,550,490],[21,550,460],[22,550,438],[23,550,418],[24,526,382],[25,485,337],[26,448,299],[27,415,267],[28,385,239],[29,359,215],[30,335,194],[31,313,175],[32,294,159],[33,276,145],[34,260,132],[35,245,121],[36,232,111]],"18K9":[[18,550,550],[19,550,523],[20,550,490],[21,550,460],[22,550,438],[23,550,418],[24,550,396],[25,550,377],[26,538,354],[27,498,315],[28,463,282],[29,431,254],[30,402,229],[31,376,207],[32,353,188],[33,332,171],[34,312,156],[35,294,143],[36,278,132]],"18K10":[[18,550,550],[19,550,523],[20,550,490],[21,550,460],[22,550,438],[23,550,418],[24,550,396],[25,550,377],[26,550,361],[27,550,347],[28,548,331],[29,511,298],[30,477,269],[31,446,243],[32,418,221],[33,393,201],[34,370,184],[35,349,168],[36,330,154],[37,312,141],[38,296,128],[39,280,118],[40,266,109]],"20K3":[[19,550,550],[20,517,517],[21,468,453],[22,426,393],[23,389,344],[24,357,302],[25,329,266],[26,304,236],[27,281,211],[28,261,189],[29,243,170],[30,227,153],[31,212,138],[32,199,126],[33,187,114],[34,176,105],[35,166,96],[36,157,88],[37,148,81],[38,141,74],[39,133,69],[40,127,64]],"20K4":[[19,550,550],[20,550,550],[21,550,520],[22,514,461],[23,469,402],[24,430,353],[25,396,312],[26,366,277],[27,339,247],[28,315,221],[29,293,199],[30,274,179],[31,256,162],[32,240,147],[33,226,134],[34,212,122],[35,200,112],[36,189,103],[37,179,95],[38,170,87],[39,161,81],[40,153,75]],"20K5":[[19,550,550],[20,550,550],[21,550,520],[22,550,490],[23,529,451],[24,485,396],[25,446,350],[26,412,310],[27,382,277],[28,355,248],[29,330,223],[30,308,201],[31,289,182],[32,271,165],[33,254,150],[34,239,137],[35,226,126],[36,213,115],[37,202,106],[38,191,98],[39,181,90],[40,172,84]],"20K6":[[19,550,550],[20,550,550],[21,550,520],[22,550,490],[23,550,468],[24,528,430],[25,486,380],[26,449,337],[27,416,301],[28,386,269],[29,360,242],[30,336,218],[31,314,198],[32,295,179],[33,277,163],[34,261,149],[35,246,137],[36,232,125],[37,220,115],[38,208,106],[39,198,98],[40,188,91]],"20K7":[[19,550,550],[20,550,550],[21,550,520],[22,550,490],[23,550,468],[24,550,448],[25,541,421],[26,500,373],[27,463,333],[28,430,298],[29,401,268],[30,374,242],[31,350,219],[32,328,199],[33,309,181],[34,290,165],[35,274,151],[36,259,139],[37,245,128],[38,232,118],[39,220,109],[40,209,101]],"20K9":[[19,550,550],[20,550,550],[21,550,520],[22,550,490],[23,550,468],[24,550,448],[25,550,426],[26,550,405],[27,550,389],[28,517,353],[29,482,317],[30,450,286],[31,421,259],[32,395,235],[33,371,214],[34,349,195],[35,329,179],[36,311,164],[37,294,151],[38,279,139],[39,265,129],[40,251,119]],"20K10":[[19,550,550],[20,550,550],[21,550,520],[22,550,490],[23,550,468],[24,550,448],[25,550,426],[26,550,405],[27,550,389],[28,550,375],[29,550,359],[30,533,336],[31,499,304],[32,468,276],[33,440,251],[34,414,229],[35,390,210],[36,369,193],[37,349,178],[38,331,164],[39,314,151],[40,298,140]],"22K4":[[21,550,550],[22,550,548],[23,518,491],[24,475,431],[25,438,381],[26,404,338],[27,374,301],[28,348,270],[29,324,242],[30,302,219],[31,283,198],[32,265,180],[33,249,164],[34,235,149],[35,221,137],[36,209,126],[37,198,116],[38,187,107],[39,178,98],[40,169,91],[41,161,85],[42,153,79],[43,146,73],[44,139,68]],"22K5":[[21,550,550],[22,550,548],[23,550,518],[24,536,483],[25,493,427],[26,455,379],[27,422,337],[28,392,302],[29,365,272],[30,341,245],[31,319,222],[32,299,201],[33,281,183],[34,265,167],[35,249,153],[36,236,141],[37,223,130],[38,211,119],[39,200,110],[40,190,102],[41,181,95],[42,173,88],[43,165,82],[44,157,76]],"22K6":[[21,550,550],[22,550,548],[23,550,518],[24,550,495],[25,537,464],[26,496,411],[27,459,367],[28,427,328],[29,398,295],[30,371,266],[31,347,241],[32,326,219],[33,306,199],[34,288,182],[35,272,167],[36,257,153],[37,243,141],[38,230,130],[39,218,120],[40,207,111],[41,197,103],[42,188,96],[43,179,89],[44,171,83]],"22K7":[[21,550,550],[22,550,548],[23,550,518],[24,550,495],[25,550,474],[26,550,454],[27,512,406],[28,475,364],[29,443,327],[30,413,295],[31,387,267],[32,363,242],[33,341,221],[34,321,202],[35,303,185],[36,286,169],[37,271,156],[38,256,144],[39,243,133],[40,231,123],[41,220,114],[42,209,106],[43,200,99],[44,191,92]],"22K9":[[21,550,550],[22,550,548],[23,550,518],[24,550,495],[25,550,474],[26,550,454],[27,550,432],[28,550,413],[29,532,387],[30,497,349],[31,465,316],[32,436,287],[33,410,261],[34,386,239],[35,364,219],[36,344,201],[37,325,185],[38,308,170],[39,292,157],[40,278,146],[41,264,135],[42,252,126],[43,240,117],[44,229,109]],"22K10":[[21,550,550],[22,550,548],[23,550,518],[24,550,495],[25,550,474],[26,550,454],[27,550,432],[28,550,413],[29,550,399],[30,550,385],[31,550,369],[32,517,337],[33,486,307],[34,458,280],[35,432,257],[36,408,236],[37,386,217],[38,366,200],[39,347,185],[40,330,171],[41,314,159],[42,299,148],[43,285,138],[44,272,128]],"22K11":[[21,550,550],[22,550,548],[23,550,518],[24,550,495],[25,550,474],[26,550,454],[27,550,432],[28,550,413],[29,550,399],[30,550,385],[31,550,369],[32,549,355],[33,532,334],[34,516,314],[35,494,292],[36,467,269],[37,442,247],[38,419,228],[39,397,211],[40,377,195],[41,359,181],[42,342,168],[43,326,157],[44,311,146]],"24K4":[[23,550,550],[24,520,516],[25,479,456],[26,442,405],[27,410,361],[28,381,323],[29,354,290],[30,331,262],[31,310,237],[32,290,215],[33,273,196],[34,257,179],[35,242,164],[36,229,150],[37,216,138],[38,205,128],[39,195,118],[40,185,109],[41,176,101],[42,168,94],[43,160,88],[44,153,82],[45,146,76],[46,139,71],[47,133,67],[48,128,63]],"24K5":[[23,550,550],[24,550,544],[25,540,511],[26,499,453],[27,462,404],[28,429,362],[29,400,325],[30,373,293],[31,349,266],[32,327,241],[33,308,220],[34,290,201],[35,273,184],[36,258,169],[37,244,155],[38,231,143],[39,219,132],[40,208,122],[41,198,114],[42,189,106],[43,180,98],[44,172,92],[45,164,86],[46,157,80],[47,150,75],[48,144,70]],"24K6":[[23,550,550],[24,550,544],[25,550,520],[26,543,493],[27,503,439],[28,467,393],[29,435,354],[30,406,319],[31,380,289],[32,357,262],[33,335,239],[34,315,218],[35,297,200],[36,281,183],[37,266,169],[38,252,156],[39,239,144],[40,227,133],[41,216,124],[42,206,115],[43,196,107],[44,187,100],[45,179,93],[46,171,87],[47,164,82],[48,157,77]],"24K7":[[23,550,550],[24,550,544],[25,550,520],[26,550,499],[27,550,479],[28,521,436],[29,485,392],[30,453,353],[31,424,320],[32,397,290],[33,373,265],[34,351,242],[35,331,221],[36,313,203],[37,296,187],[38,281,172],[39,266,159],[40,253,148],[41,241,137],[42,229,127],[43,219,118],[44,209,110],[45,199,103],[46,191,97],[47,183,90],[48,175,85]],"24K8":[[23,550,550],[24,550,544],[25,550,520],[26,550,499],[27,550,479],[28,550,456],[29,536,429],[30,500,387],[31,468,350],[32,439,318],[33,413,289],[34,388,264],[35,366,242],[36,346,222],[37,327,205],[38,310,189],[39,294,174],[40,280,161],[41,266,150],[42,253,139],[43,242,130],[44,231,121],[45,220,113],[46,211,106],[47,202,99],[48,194,93]],"24K9":[[23,550,550],[24,550,544],[25,550,520],[26,550,499],[27,550,479],[28,550,456],[29,550,436],[30,544,419],[31,510,379],[32,478,344],[33,449,313],[34,423,286],[35,399,262],[36,377,241],[37,356,222],[38,338,204],[39,320,189],[40,304,175],[41,290,162],[42,276,151],[43,263,140],[44,251,131],[45,240,122],[46,230,114],[47,220,107],[48,211,100]],"24K10":[[23,550,550],[24,550,544],[25,550,520],[26,550,499],[27,550,479],[28,550,456],[29,550,436],[30,550,422],[31,550,410],[32,549,393],[33,532,368],[34,502,337],[35,473,308],[36,447,283],[37,423,260],[38,401,240],[39,380,222],[40,361,206],[41,344,191],[42,327,177],[43,312,165],[44,298,154],[45,285,144],[46,272,135],[47,261,126],[48,250,118]],"24K12":[[23,550,550],[24,550,544],[25,550,520],[26,550,499],[27,550,479],[28,550,456],[29,550,436],[30,550,422],[31,550,410],[32,549,393],[33,532,368],[34,516,344],[35,501,324],[36,487,306],[37,474,290],[38,461,275],[39,449,261],[40,438,247],[41,427,235],[42,417,224],[43,406,213],[44,387,199],[45,370,185],[46,354,174],[47,339,163],[48,325,153]],"26K5":[[25,550,550],[26,542,535],[27,502,477],[28,466,427],[29,434,384],[30,405,346],[31,379,314],[32,356,285],[33,334,259],[34,315,237],[35,297,217],[36,280,199],[37,265,183],[38,251,169],[39,238,156],[40,227,145],[41,215,134],[42,205,125],[43,196,116],[44,187,108],[45,179,101],[46,171,95],[47,164,89],[48,157,83],[49,150,78],[50,144,73],[51,139,69],[52,133,65]],"26K6":[[25,550,550],[26,550,541],[27,547,519],[28,508,464],[29,473,417],[30,441,377],[31,413,341],[32,387,309],[33,364,282],[34,343,257],[35,323,236],[36,305,216],[37,289,199],[38,274,184],[39,260,170],[40,247,157],[41,235,146],[42,224,136],[43,213,126],[44,204,118],[45,194,110],[46,186,103],[47,178,96],[48,171,90],[49,164,85],[50,157,80],[51,151,75],[52,145,71]],"26K7":[[25,550,550],[26,550,541],[27,550,522],[28,550,501],[29,527,463],[30,492,417],[31,460,378],[32,432,343],[33,406,312],[34,382,285],[35,360,261],[36,340,240],[37,322,221],[38,305,204],[39,289,188],[40,275,174],[41,262,162],[42,249,150],[43,238,140],[44,227,131],[45,217,122],[46,207,114],[47,199,107],[48,190,100],[49,183,94],[50,175,89],[51,168,83],[52,162,79]],"26K8":[[25,550,550],[26,550,541],[27,550,522],[28,550,501],[29,550,479],[30,544,457],[31,509,413],[32,477,375],[33,448,342],[34,422,312],[35,398,286],[36,376,263],[37,356,242],[38,337,223],[39,320,206],[40,304,191],[41,289,177],[42,275,164],[43,263,153],[44,251,143],[45,240,133],[46,229,125],[47,219,117],[48,210,110],[49,202,103],[50,194,97],[51,186,91],[52,179,86]],"26K9":[[25,550,550],[26,550,541],[27,550,522],[28,550,501],[29,550,479],[30,550,459],[31,550,444],[32,519,407],[33,488,370],[34,459,338],[35,433,310],[36,409,284],[37,387,262],[38,367,241],[39,348,223],[40,331,207],[41,315,191],[42,300,178],[43,286,166],[44,273,155],[45,261,145],[46,249,135],[47,238,127],[48,229,119],[49,220,112],[50,211,105],[51,203,99],[52,195,93]],"26K10":[[25,550,550],[26,550,541],[27,550,522],[28,550,501],[29,550,479],[30,550,459],[31,550,444],[32,549,431],[33,532,404],[34,516,378],[35,501,356],[36,486,334],[37,460,308],[38,436,284],[39,413,262],[40,393,243],[41,374,225],[42,356,210],[43,339,195],[44,324,182],[45,310,170],[46,296,159],[47,284,149],[48,272,140],[49,261,131],[50,250,124],[51,241,116],[52,231,110]],"26K12":[[25,550,550],[26,550,541],[27,550,522],[28,550,501],[29,550,479],[30,550,459],[31,550,444],[32,549,431],[33,532,404],[34,516,378],[35,501,356],[36,487,334],[37,474,315],[38,461,299],[39,449,283],[40,438,269],[41,427,256],[42,417,244],[43,407,232],[44,398,222],[45,389,212],[46,380,203],[47,369,192],[48,353,180],[49,339,169],[50,325,159],[51,313,150],[52,301,142]],"28K6":[[27,550,550],[28,548,541],[29,511,486],[30,477,439],[31,446,397],[32,418,361],[33,393,329],[34,370,300],[35,349,275],[36,330,252],[37,312,232],[38,296,214],[39,280,198],[40,266,183],[41,253,170],[42,241,158],[43,230,147],[44,220,137],[45,210,128],[46,201,120],[47,192,112],[48,184,105],[49,177,99],[50,170,93],[51,163,88],[52,157,83],[53,151,78],[54,145,74],[55,140,70],[56,135,66]],"28K7":[[27,550,550],[28,550,543],[29,550,522],[30,531,486],[31,497,440],[32,466,400],[33,438,364],[34,412,333],[35,389,305],[36,367,280],[37,348,257],[38,329,237],[39,313,219],[40,297,203],[41,283,189],[42,269,175],[43,257,163],[44,245,152],[45,234,142],[46,224,133],[47,214,125],[48,206,117],[49,197,110],[50,189,103],[51,182,97],[52,175,92],[53,168,87],[54,162,82],[55,156,77],[56,151,73]],"28K8":[[27,550,550],[28,550,543],[29,550,522],[30,550,500],[31,550,480],[32,515,438],[33,484,399],[34,456,364],[35,430,333],[36,406,306],[37,384,282],[38,364,260],[39,346,240],[40,328,222],[41,312,206],[42,297,192],[43,284,179],[44,271,167],[45,259,156],[46,248,146],[47,237,136],[48,227,128],[49,218,120],[50,209,113],[51,201,106],[52,193,100],[53,186,95],[54,179,89],[55,173,85],[56,166,80]],"28K9":[[27,550,550],[28,550,543],[29,550,522],[30,550,500],[31,550,480],[32,549,463],[33,527,432],[34,496,395],[35,468,361],[36,442,332],[37,418,305],[38,396,282],[39,376,260],[40,357,241],[41,340,224],[42,324,208],[43,309,194],[44,295,181],[45,282,169],[46,270,158],[47,258,148],[48,247,139],[49,237,130],[50,228,123],[51,219,115],[52,210,109],[53,203,103],[54,195,97],[55,188,92],[56,181,87]],"28K10":[[27,550,550],[28,550,543],[29,550,522],[30,550,500],[31,550,480],[32,549,463],[33,532,435],[34,516,410],[35,501,389],[36,487,366],[37,474,344],[38,461,325],[39,447,306],[40,424,284],[41,404,263],[42,384,245],[43,367,228],[44,350,212],[45,334,198],[46,320,186],[47,306,174],[48,294,163],[49,282,153],[50,270,144],[51,260,136],[52,250,128],[53,240,121],[54,232,114],[55,223,108],[56,215,102]],"28K12":[[27,550,550],[28,550,543],[29,550,522],[30,550,500],[31,550,480],[32,549,463],[33,532,435],[34,516,410],[35,501,389],[36,487,366],[37,474,344],[38,461,325],[39,449,308],[40,438,291],[41,427,277],[42,417,264],[43,407,252],[44,398,240],[45,389,229],[46,380,219],[47,372,210],[48,365,201],[49,357,193],[50,350,185],[51,338,175],[52,325,165],[53,313,156],[54,301,147],[55,290,139],[56,280,132]],"30K7":[[29,550,550],[30,550,543],[31,534,508],[32,501,461],[33,471,420],[34,443,384],[35,418,351],[36,395,323],[37,373,297],[38,354,274],[39,336,253],[40,319,234],[41,303,217],[42,289,202],[43,276,188],[44,263,176],[45,251,164],[46,241,153],[47,230,144],[48,221,135],[49,212,127],[50,203,119],[51,195,112],[52,188,106],[53,181,100],[54,174,94],[55,168,89],[56,162,84],[57,156,80],[58,151,76],[59,146,72],[60,141,69]],"30K8":[[29,550,550],[30,550,543],[31,550,520],[32,549,500],[33,520,460],[34,490,420],[35,462,384],[36,436,353],[37,413,325],[38,391,300],[39,371,277],[40,353,256],[41,335,238],[42,320,221],[43,305,206],[44,291,192],[45,278,179],[46,266,168],[47,255,157],[48,244,148],[49,234,139],[50,225,130],[51,216,123],[52,208,116],[53,200,109],[54,192,103],[55,185,98],[56,179,92],[57,173,88],[58,167,83],[59,161,79],[60,156,75]],"30K9":[[29,550,550],[30,550,543],[31,550,520],[32,549,500],[33,532,468],[34,516,441],[35,501,415],[36,475,383],[37,449,352],[38,426,325],[39,404,300],[40,384,278],[41,365,258],[42,348,240],[43,332,223],[44,317,208],[45,303,195],[46,290,182],[47,277,171],[48,266,160],[49,255,150],[50,245,141],[51,235,133],[52,226,126],[53,218,119],[54,209,112],[55,202,106],[56,195,100],[57,188,95],[58,181,90],[59,175,86],[60,169,81]],"30K10":[[29,550,550],[30,550,543],[31,550,520],[32,549,500],[33,532,468],[34,516,441],[35,501,415],[36,487,392],[37,474,374],[38,461,353],[39,449,333],[40,438,315],[41,427,300],[42,413,282],[43,394,263],[44,376,245],[45,359,229],[46,344,214],[47,329,201],[48,315,188],[49,303,177],[50,291,166],[51,279,157],[52,268,148],[53,258,140],[54,249,132],[55,240,125],[56,231,118],[57,223,112],[58,215,106],[59,208,101],[60,201,96]],"30K11":[[29,550,550],[30,550,543],[31,550,520],[32,549,500],[33,532,468],[34,516,441],[35,501,415],[36,487,392],[37,474,374],[38,461,353],[39,449,333],[40,438,315],[41,427,300],[42,417,284],[43,407,270],[44,398,258],[45,389,246],[46,380,236],[47,372,226],[48,362,215],[49,347,202],[50,333,190],[51,320,179],[52,308,169],[53,296,159],[54,285,150],[55,275,142],[56,265,135],[57,256,128],[58,247,121],[59,239,115],[60,231,109]],"30K12":[[29,550,550],[30,550,543],[31,550,520],[32,549,500],[33,532,468],[34,516,441],[35,501,415],[36,487,392],[37,474,374],[38,461,353],[39,449,333],[40,438,315],[41,427,300],[42,417,284],[43,407,270],[44,398,258],[45,389,246],[46,380,236],[47,372,226],[48,365,216],[49,357,207],[50,350,199],[51,343,192],[52,336,184],[53,330,177],[54,324,170],[55,312,161],[56,301,153],[57,290,145],[58,280,137],[59,271,130],[60,262,124]]};

// ═══════════════════════════════════════════════════════════════════════
// BEAM ANALYSIS ENGINE
// ═══════════════════════════════════════════════════════════════════════
const NUM_POINTS = 500;

function analyzeBeam(span, uniformLoads, pointLoads, E_ksi, I_in4, triangularLoads = []) {
  const L = span; // ft
  const n = NUM_POINTS;
  const dx = L / n;
  const x = Array.from({ length: n + 1 }, (_, i) => i * dx);

  // Compute shear and moment at each point using superposition
  const V = new Float64Array(n + 1);
  const M = new Float64Array(n + 1);

  // Process each uniform load: w plf from a to b
  for (const ul of uniformLoads) {
    const w = ul.w; // plf
    const a = ul.a; // ft start
    const b = ul.b; // ft end
    if (w === 0 || a >= b) continue;

    const totalForce = w * (b - a);
    const Rb = totalForce * (a + b) / (2 * L);
    const Ra = totalForce - Rb;

    for (let i = 0; i <= n; i++) {
      const xi = x[i];
      let v = Ra;
      let m = Ra * xi;

      if (xi > a) {
        const loadLen = Math.min(xi, b) - a;
        v -= w * loadLen;
        m -= w * loadLen * (xi - a - loadLen / 2);
      }

      V[i] += v;
      M[i] += m;
    }
  }

  // Process triangular loads: linearly varying from wStart to wEnd over [a, b]
  for (const tl of triangularLoads) {
    const { wStart, wEnd, a, b } = tl;
    if (a >= b) continue;
    const len = b - a;
    // Total force = (wStart + wEnd) / 2 * len
    const totalForce = (wStart + wEnd) / 2 * len;
    // Centroid from a: (len/3) * (wStart + 2*wEnd) / (wStart + wEnd)
    const centroid = a + (len / 3) * (wStart + 2 * wEnd) / (wStart + wEnd || 1);
    const Rb = totalForce * centroid / L;
    const Ra = totalForce - Rb;

    for (let i = 0; i <= n; i++) {
      const xi = x[i];
      let v = Ra;
      let m = Ra * xi;

      if (xi > a) {
        const xLocal = Math.min(xi, b) - a;
        // w(x) = wStart + (wEnd - wStart) * x / len
        // Integral of w from 0 to xLocal = wStart * xLocal + (wEnd - wStart) * xLocal^2 / (2 * len)
        const loadArea = wStart * xLocal + (wEnd - wStart) * xLocal * xLocal / (2 * len);
        // Centroid of loaded portion from a:
        // integral of w*x dx / loadArea
        const loadMoment = wStart * xLocal * xLocal / 2 + (wEnd - wStart) * xLocal * xLocal * xLocal / (3 * len);
        const loadCentroid = loadArea > 0 ? loadMoment / loadArea : xLocal / 2;
        v -= loadArea;
        m -= loadArea * (xi - a - loadCentroid);
      }

      V[i] += v;
      M[i] += m;
    }
  }

  // Process each point load: P at location d
  for (const pl of pointLoads) {
    const P = pl.P; // lb
    const d = pl.d; // ft location
    if (P === 0) continue;

    const Ra = P * (L - d) / L;

    for (let i = 0; i <= n; i++) {
      const xi = x[i];
      let v = Ra;
      let m = Ra * xi;

      if (xi > d) {
        v -= P;
        m -= P * (xi - d);
      }

      V[i] += v;
      M[i] += m;
    }
  }

  // Find max absolute values
  let maxV = 0, maxM = 0;
  for (let i = 0; i <= n; i++) {
    if (Math.abs(V[i]) > Math.abs(maxV)) maxV = V[i];
    if (Math.abs(M[i]) > Math.abs(maxM)) maxM = M[i];
  }

  // Compute deflection using conjugate beam / numerical integration
  // M/(EI) is the "load" on the conjugate beam
  // E in ksi = E * 1000 psi; I in in^4
  // M is in lb-ft; need consistent units
  // delta = integral of (M * x_bar) / (E*I) ... use double integration
  // Convert: M in lb-ft, E in psi (ksi*1000), I in in^4
  // M in lb-in = M_lb_ft * 12
  // EI in lb-in^2 = E_ksi * 1000 * I_in4

  const EI = E_ksi * 1000 * I_in4; // lb-in^2
  const dxIn = dx * 12; // inches

  // M/EI curvature at each point (using M in lb-in)
  const kappa = new Float64Array(n + 1);
  for (let i = 0; i <= n; i++) {
    kappa[i] = (M[i] * 12) / EI; // 1/in
  }

  // First integration: slope (theta)
  const theta = new Float64Array(n + 1);
  theta[0] = 0;
  for (let i = 1; i <= n; i++) {
    theta[i] = theta[i - 1] + (kappa[i - 1] + kappa[i]) / 2 * dxIn;
  }

  // Second integration: deflection (y)
  const y = new Float64Array(n + 1);
  y[0] = 0;
  for (let i = 1; i <= n; i++) {
    y[i] = y[i - 1] + (theta[i - 1] + theta[i]) / 2 * dxIn;
  }

  // Apply boundary conditions: y(0) = 0, y(L) = 0
  // Correct by subtracting linear interpolation
  const yL = y[n];
  const defl = new Float64Array(n + 1);
  let maxDefl = 0;
  for (let i = 0; i <= n; i++) {
    defl[i] = y[i] - (yL * i) / n;
    if (Math.abs(defl[i]) > Math.abs(maxDefl)) maxDefl = defl[i];
  }

  return {
    x: Array.from(x),
    V: Array.from(V),
    M: Array.from(M),
    defl: Array.from(defl),
    maxV: Math.abs(maxV),
    maxM: Math.abs(maxM),
    maxDefl: Math.abs(maxDefl),
  };
}

// ═══════════════════════════════════════════════════════════════════════
// DIAGRAM COMPONENT (D3-based)
// ═══════════════════════════════════════════════════════════════════════
function Diagram({ x, y, title, unit, color, capacityLine, fillColor, overlayX, overlayY, yTickInterval }) {
  const svgRef = useRef(null);
  const tooltipRef = useRef(null);
  const containerRef = useRef(null);
  const [dims, setDims] = useState({ width: 400, height: 200 });
  const hasOverlay = overlayX && overlayY && overlayY.length > 0;

  useEffect(() => {
    const ro = new ResizeObserver(entries => {
      for (const e of entries) {
        const w = e.contentRect.width;
        if (w > 50) setDims({ width: w, height: 200 });
      }
    });
    if (containerRef.current) ro.observe(containerRef.current);
    return () => ro.disconnect();
  }, []);

  useEffect(() => {
    if (!svgRef.current || !x || x.length === 0) return;

    const margin = { top: 28, right: 16, bottom: 32, left: 64 };
    const w = dims.width - margin.left - margin.right;
    const h = dims.height - margin.top - margin.bottom;

    const svg = d3.select(svgRef.current);
    svg.selectAll("*").remove();
    svg.attr("width", dims.width).attr("height", dims.height);

    const g = svg.append("g").attr("transform", `translate(${margin.left},${margin.top})`);

    // Compute y domain including overlay data
    let yMin = Math.min(0, d3.min(y));
    let yMax = Math.max(0, d3.max(y));
    if (hasOverlay) {
      yMin = Math.min(yMin, d3.min(overlayY));
      yMax = Math.max(yMax, d3.max(overlayY));
    }

    const spanLen = x[x.length - 1];
    const xScale = d3.scaleLinear().domain([0, spanLen]).range([0, w]);

    // Snap y domain to tick interval if provided
    const pad = (yMax - yMin) * 0.12 || 1;
    let yDomMin = yMin - pad;
    let yDomMax = yMax + pad;
    if (yTickInterval) {
      yDomMin = Math.floor(yDomMin / yTickInterval) * yTickInterval;
      yDomMax = Math.ceil(yDomMax / yTickInterval) * yTickInterval;
    }
    const yScale = d3.scaleLinear().domain([yDomMin, yDomMax]).range([h, 0]);

    // Zero line
    g.append("line")
      .attr("x1", 0).attr("x2", w)
      .attr("y1", yScale(0)).attr("y2", yScale(0))
      .attr("stroke", "#64748b").attr("stroke-width", 1)
      .attr("stroke-dasharray", "4,3");

    const isDeflection = unit === "in";

    // ── Exceedance highlight zones with % labels ──
    if (hasOverlay) {
      const bandH = h / 8;
      const bandY = h - bandH;

      // Helper: interpolate capacity at a given x position
      const getCapAt = (xi) => {
        if (!overlayX || !overlayY) return 0;
        const idx = overlayX.findIndex(ox => ox >= xi);
        if (idx <= 0) return Math.abs(overlayY[0] || 0);
        const t = (xi - overlayX[idx-1]) / (overlayX[idx] - overlayX[idx-1] || 1);
        return Math.abs(overlayY[idx-1] + t * (overlayY[idx] - overlayY[idx-1]));
      };

      // Collect exceedance zones: [{start, end, maxDev}]
      const zones = [];
      let inExceed = false;
      let exceedStart = 0;
      let zoneMaxDev = 0;
      const step = Math.max(1, Math.floor(y.length / 500));

      for (let i = 0; i <= y.length; i += (i < y.length ? step : 0)) {
        if (i >= y.length) i = y.length - 1;
        const xi = x[i];
        const demandVal = Math.abs(y[i]);
        const capVal = getCapAt(xi);
        const exceeds = demandVal > capVal * 1.001;
        const dev = demandVal - capVal;

        if (exceeds && !inExceed) {
          exceedStart = xi;
          zoneMaxDev = dev;
          inExceed = true;
        } else if (exceeds && inExceed) {
          zoneMaxDev = Math.max(zoneMaxDev, dev);
        } else if (!exceeds && inExceed) {
          zones.push({ start: exceedStart, end: xi, maxDev: zoneMaxDev });
          inExceed = false;
        }
        if (i === y.length - 1) {
          if (inExceed) zones.push({ start: exceedStart, end: xi, maxDev: zoneMaxDev });
          break;
        }
      }

      // Draw zones with deviation value labels
      for (const zone of zones) {
        const zx1 = xScale(zone.start);
        const zx2 = xScale(zone.end);
        const zw = Math.max(2, zx2 - zx1);
        g.append("rect")
          .attr("x", zx1).attr("y", bandY)
          .attr("width", zw).attr("height", bandH)
          .attr("fill", "#ef4444").attr("opacity", 0.15).attr("rx", 2);
        // Format deviation value based on unit
        const devLabel = isDeflection
          ? "+" + zone.maxDev.toFixed(3) + " in"
          : "+" + Math.round(zone.maxDev).toLocaleString() + " " + unit;
        g.append("text")
          .attr("x", zx1 + zw / 2).attr("y", bandY + bandH / 2 + 3.5)
          .attr("text-anchor", "middle")
          .attr("fill", "#ef4444").attr("font-size", "9px").attr("font-weight", 700)
          .text(devLabel);
      }
    }

    // ── Overlay curve (capacity) ──
    if (hasOverlay) {
      const overlayLine = d3.line()
        .x((_, i) => xScale(overlayX[i]))
        .y(d => yScale(d))
        .curve(d3.curveMonotoneX);

      const overlayArea = d3.area()
        .x((_, i) => xScale(overlayX[i]))
        .y0(yScale(0))
        .y1(d => yScale(d))
        .curve(d3.curveMonotoneX);

      g.append("path").datum(overlayY).attr("d", overlayArea)
        .attr("fill", "rgba(239,68,68,0.05)").attr("stroke", "none");

      g.append("path").datum(overlayY).attr("d", overlayLine)
        .attr("fill", "none").attr("stroke", "#ef4444")
        .attr("stroke-width", 1.5).attr("stroke-dasharray", "6,3").attr("opacity", 0.8);
    }

    // ── Primary area + line ──
    const area = d3.area()
      .x((_, i) => xScale(x[i]))
      .y0(yScale(0))
      .y1(d => yScale(d))
      .curve(d3.curveMonotoneX);

    g.append("path").datum(y).attr("d", area)
      .attr("fill", fillColor || "rgba(59,130,246,0.12)").attr("stroke", "none");

    const line = d3.line()
      .x((_, i) => xScale(x[i]))
      .y(d => yScale(d))
      .curve(d3.curveMonotoneX);

    g.append("path").datum(y).attr("d", line)
      .attr("fill", "none").attr("stroke", color || "#3b82f6").attr("stroke-width", 2);

    // ── Axes ──
    // X axis: 1 ft increments
    const xAxis = d3.axisBottom(xScale)
      .tickValues(d3.range(0, spanLen + 0.5, 1))
      .tickFormat(d => d % 5 === 0 || d === 0 || d === Math.round(spanLen) ? d + "'" : "");

    // Y axis: use provided interval or auto
    let yAxis;
    if (yTickInterval) {
      const ticks = [];
      for (let v = yDomMin; v <= yDomMax + yTickInterval * 0.01; v += yTickInterval) {
        ticks.push(Math.round(v * 1000) / 1000);
      }
      yAxis = d3.axisLeft(yScale).tickValues(ticks);
    } else {
      yAxis = d3.axisLeft(yScale).ticks(6);
    }

    // Format y-axis numbers
    if (isDeflection) {
      yAxis.tickFormat(d => d === 0 ? "0" : d.toFixed(3));
    } else {
      yAxis.tickFormat(d3.format(",.0f"));
    }

    const xAxisG = g.append("g").attr("transform", `translate(0,${h})`).call(xAxis);
    xAxisG.selectAll("text").attr("fill", "#94a3b8").attr("font-size", "9px");
    // Minor ticks (no label) get shorter
    xAxisG.selectAll(".tick").each(function(d) {
      const text = d3.select(this).select("text").text();
      if (!text) d3.select(this).select("line").attr("y2", 3);
    });

    const yAxisG = g.append("g").call(yAxis);
    yAxisG.selectAll("text").attr("fill", "#94a3b8").attr("font-size", "10px");

    g.selectAll(".domain").attr("stroke", "#334155");
    g.selectAll(".tick line").attr("stroke", "#334155");

    // ── Title + Legend ──
    svg.append("text")
      .attr("x", margin.left + w / 2).attr("y", 14)
      .attr("text-anchor", "middle")
      .attr("fill", "#e2e8f0").attr("font-size", "12px").attr("font-weight", 600)
      .text(`${title} (${unit})`);

    if (hasOverlay) {
      const lx = 8;
      g.append("line").attr("x1", lx).attr("x2", lx + 18).attr("y1", -10).attr("y2", -10)
        .attr("stroke", "#ef4444").attr("stroke-width", 1.5).attr("stroke-dasharray", "4,2");
      g.append("text").attr("x", lx + 22).attr("y", -7)
        .attr("fill", "#ef4444").attr("font-size", "9px").text("Capacity");
      g.append("line").attr("x1", lx + 72).attr("x2", lx + 90).attr("y1", -10).attr("y2", -10)
        .attr("stroke", color || "#3b82f6").attr("stroke-width", 2);
      g.append("text").attr("x", lx + 94).attr("y", -7)
        .attr("fill", color || "#3b82f6").attr("font-size", "9px").text("Demand");
    }

    // ── Interactive crosshair (only if overlay) ──
    if (hasOverlay) {
      const crossV = g.append("line").attr("y1", 0).attr("y2", h)
        .attr("stroke", "#94a3b8").attr("stroke-width", 0.8).attr("stroke-dasharray", "3,3").style("display", "none");
      const crossH = g.append("line").attr("x1", 0).attr("x2", w)
        .attr("stroke", "#94a3b8").attr("stroke-width", 0.8).attr("stroke-dasharray", "3,3").style("display", "none");

      const dotDemand = g.append("circle").attr("r", 4).attr("fill", color || "#3b82f6").attr("stroke", "#fff").attr("stroke-width", 1).style("display", "none");
      const dotCap = g.append("circle").attr("r", 4).attr("fill", "#ef4444").attr("stroke", "#fff").attr("stroke-width", 1).style("display", "none");

      const tooltip = g.append("g").style("display", "none");
      const tooltipBg = tooltip.append("rect").attr("rx", 5).attr("ry", 5).attr("fill", "#1e293b").attr("stroke", "#475569").attr("stroke-width", 0.5).attr("opacity", 0.95);
      const tooltipText1 = tooltip.append("text").attr("fill", "#94a3b8").attr("font-size", "10px").attr("font-weight", 500);
      const tooltipText2 = tooltip.append("text").attr("fill", color || "#3b82f6").attr("font-size", "10px").attr("font-weight", 600);
      const tooltipText3 = tooltip.append("text").attr("fill", "#ef4444").attr("font-size", "10px").attr("font-weight", 600);

      const fmtVal = isDeflection ? (v => v.toFixed(4) + " in") : (v => Math.round(v).toLocaleString() + " " + unit);

      // Bisector for finding closest data point
      const bisect = d3.bisector(d => d).left;

      const overlay = g.append("rect").attr("width", w).attr("height", h).attr("fill", "none").attr("pointer-events", "all").style("cursor", "crosshair");

      overlay.on("mousemove", function(event) {
        const [mx] = d3.pointer(event, this);
        const xVal = xScale.invert(mx);
        if (xVal < 0 || xVal > spanLen) return;

        // Find demand value
        const idx = bisect(x, xVal);
        const i0 = Math.max(0, idx - 1);
        const i1 = Math.min(x.length - 1, idx);
        const t = (x[i1] - x[i0]) > 0 ? (xVal - x[i0]) / (x[i1] - x[i0]) : 0;
        const demandVal = y[i0] + t * (y[i1] - y[i0]);

        // Find capacity value
        const ci = bisect(overlayX, xVal);
        const ci0 = Math.max(0, ci - 1);
        const ci1 = Math.min(overlayX.length - 1, ci);
        const ct = (overlayX[ci1] - overlayX[ci0]) > 0 ? (xVal - overlayX[ci0]) / (overlayX[ci1] - overlayX[ci0]) : 0;
        const capVal = overlayY[ci0] + ct * (overlayY[ci1] - overlayY[ci0]);

        const px = xScale(xVal);
        crossV.attr("x1", px).attr("x2", px).style("display", null);
        crossH.attr("y1", yScale(demandVal)).attr("y2", yScale(demandVal)).style("display", null);
        dotDemand.attr("cx", px).attr("cy", yScale(demandVal)).style("display", null);
        dotCap.attr("cx", px).attr("cy", yScale(capVal)).style("display", null);

        // Tooltip
        tooltip.style("display", null);
        const xLabel = "x = " + xVal.toFixed(1) + " ft";
        const dLabel = "Demand: " + fmtVal(demandVal);
        const cLabel = "Capacity: " + fmtVal(capVal);
        tooltipText1.text(xLabel).attr("x", 8).attr("y", 15);
        tooltipText2.text(dLabel).attr("x", 8).attr("y", 29);
        tooltipText3.text(cLabel).attr("x", 8).attr("y", 43);

        const tw = Math.max(xLabel.length, dLabel.length, cLabel.length) * 6.5 + 16;
        const th = 52;
        tooltipBg.attr("width", tw).attr("height", th);

        let tx = px + 12;
        if (tx + tw > w) tx = px - tw - 12;
        let ty = yScale(demandVal) - th - 8;
        if (ty < 0) ty = yScale(demandVal) + 12;
        tooltip.attr("transform", `translate(${tx},${ty})`);
      });

      overlay.on("mouseleave", function() {
        crossV.style("display", "none");
        crossH.style("display", "none");
        dotDemand.style("display", "none");
        dotCap.style("display", "none");
        tooltip.style("display", "none");
      });
    }

  }, [x, y, dims, title, unit, color, capacityLine, fillColor, overlayX, overlayY, yTickInterval]);

  return (
    <div ref={containerRef} style={{ width: "100%" }}>
      <svg ref={svgRef} />
    </div>
  );
}


// ═══════════════════════════════════════════════════════════════════════
// BEAM VISUALIZATION (SVG)
// ═══════════════════════════════════════════════════════════════════════
function BeamViz({ span, uniformLoads, pointLoads, triangularLoads = [] }) {
  const svgW = 500, margin = 44;
  const bw = svgW - 2 * margin;
  const scale = bw / span;
  const maxLoadH = 55;
  const minLoadH = 12;

  // Find max magnitude for scaling
  const allMags = [
    ...uniformLoads.filter(u => u.w > 0).map(u => u.w),
    ...triangularLoads.map(t => Math.max(t.wStart || 0, t.wEnd || 0)),
  ];
  const maxMag = Math.max(...allMags, 1);

  const scaleH = (mag) => {
    if (mag <= 0) return 0;
    return minLoadH + (mag / maxMag) * (maxLoadH - minLoadH);
  };

  // Height map for stacking (discretized buckets)
  const buckets = 200;
  const heightMap = new Float64Array(buckets);

  const getOffset = (aFt, bFt) => {
    const i0 = Math.max(0, Math.floor((aFt / span) * buckets));
    const i1 = Math.min(buckets - 1, Math.floor((bFt / span) * buckets));
    let mx = 0;
    for (let i = i0; i <= i1; i++) mx = Math.max(mx, heightMap[i]);
    return mx;
  };

  const addToMap = (aFt, bFt, h) => {
    const i0 = Math.max(0, Math.floor((aFt / span) * buckets));
    const i1 = Math.min(buckets - 1, Math.floor((bFt / span) * buckets));
    for (let i = i0; i <= i1; i++) heightMap[i] += h;
  };

  // Uniform loads with stacking
  const ulRender = uniformLoads.filter(u => u.w > 0 && u.b > u.a).map((ul, i) => {
    const h = scaleH(ul.w);
    const offset = getOffset(ul.a, ul.b);
    addToMap(ul.a, ul.b, h + 5);
    return { ...ul, h, offset, idx: i };
  });

  // Triangular loads with stacking
  const triRender = triangularLoads.filter(t => (t.wStart > 0 || t.wEnd > 0) && t.b > t.a).map((tl, i) => {
    const peakMag = Math.max(tl.wStart, tl.wEnd);
    const h = scaleH(peakMag);
    const offset = getOffset(tl.a, tl.b);
    addToMap(tl.a, tl.b, h + 5);
    return { ...tl, h, offset, idx: i };
  });

  // Point loads with stacking
  const plRender = pointLoads.filter(p => p.P > 0).map((pl, i) => {
    const offset = getOffset(Math.max(0, pl.d - 0.2), Math.min(span, pl.d + 0.2));
    const arrowH = Math.max(18, offset + 22);
    addToMap(Math.max(0, pl.d - 0.3), Math.min(span, pl.d + 0.3), arrowH + 12);
    return { ...pl, arrowH, idx: i };
  });

  // SVG height from max stack
  let globalMaxH = 0;
  for (let i = 0; i < buckets; i++) globalMaxH = Math.max(globalMaxH, heightMap[i]);
  const topPad = 24;
  const bottomPad = 22;
  const svgH = topPad + Math.max(globalMaxH, 35) + 6 + bottomPad;
  const by = topPad + Math.max(globalMaxH, 35) + 4;

  const colors = ["#3b82f6", "#8b5cf6", "#06b6d4", "#10b981", "#f59e0b"];

  return (
    <svg width="100%" viewBox={`0 0 ${svgW} ${svgH}`} style={{ maxWidth: svgW }}>
      {/* Beam line */}
      <line x1={margin} y1={by} x2={margin + bw} y2={by} stroke="#94a3b8" strokeWidth={3} />
      {/* Supports */}
      <polygon points={`${margin},${by + 2} ${margin - 8},${by + 16} ${margin + 8},${by + 16}`} fill="none" stroke="#f59e0b" strokeWidth={1.5} />
      <polygon points={`${margin + bw},${by + 2} ${margin + bw - 8},${by + 16} ${margin + bw + 8},${by + 16}`} fill="none" stroke="#f59e0b" strokeWidth={1.5} />

      {/* Uniform loads — stacked & scaled */}
      {ulRender.map((ul) => {
        const x1 = margin + ul.a * scale;
        const x2 = margin + ul.b * scale;
        const baseY = by - 2 - ul.offset;
        const topY = baseY - ul.h;
        const col = colors[ul.idx % colors.length];
        const arrowCount = Math.max(2, Math.round((x2 - x1) / 20));
        return (
          <g key={`ul${ul.idx}`}>
            <rect x={x1} y={topY} width={x2 - x1} height={ul.h} fill={col} opacity={0.08} />
            <line x1={x1} y1={topY} x2={x2} y2={topY} stroke={col} strokeWidth={2} opacity={0.7} />
            <line x1={x1} y1={topY} x2={x1} y2={baseY} stroke={col} strokeWidth={0.7} opacity={0.35} />
            <line x1={x2} y1={topY} x2={x2} y2={baseY} stroke={col} strokeWidth={0.7} opacity={0.35} />
            {Array.from({ length: arrowCount }, (_, j) => {
              const ax = x1 + (j / (arrowCount - 1)) * (x2 - x1);
              return <line key={j} x1={ax} y1={topY + 2} x2={ax} y2={baseY} stroke={col} strokeWidth={1} markerEnd={`url(#arr${ul.idx % colors.length})`} />;
            })}
            <text x={(x1 + x2) / 2} y={topY - 3} textAnchor="middle" fill={col} fontSize="8" fontWeight={600}>{ul.w} plf</text>
          </g>
        );
      })}

      {/* Triangular loads — stacked & scaled */}
      {triRender.map((tl) => {
        const x1 = margin + tl.a * scale;
        const x2 = margin + tl.b * scale;
        const baseY = by - 2 - tl.offset;
        const peak = Math.max(tl.wStart, tl.wEnd, 1);
        const startH = tl.h * (tl.wStart / peak);
        const endH = tl.h * (tl.wEnd / peak);
        const cnt = Math.max(2, Math.round((x2 - x1) / 22));
        return (
          <g key={`tri${tl.idx}`}>
            <polygon
              points={`${x1},${baseY} ${x1},${baseY - startH} ${x2},${baseY - endH} ${x2},${baseY}`}
              fill="rgba(125,211,252,0.12)"
              stroke="#7dd3fc"
              strokeWidth={1.2}
            />
            {Array.from({ length: cnt }, (_, j) => {
              const frac = j / (cnt - 1);
              const ax = x1 + frac * (x2 - x1);
              const ah = startH + (endH - startH) * frac;
              if (ah < 4) return null;
              return <line key={j} x1={ax} y1={baseY - ah + 2} x2={ax} y2={baseY} stroke="#7dd3fc" strokeWidth={0.8} markerEnd="url(#arrowCyan)" />;
            })}
            {tl.wStart >= tl.wEnd
              ? <text x={x1 + 2} y={baseY - startH - 3} fill="#7dd3fc" fontSize="8" fontWeight={600}>drift {Math.round(tl.wStart)} plf</text>
              : <text x={x2 - 2} y={baseY - endH - 3} textAnchor="end" fill="#7dd3fc" fontSize="8" fontWeight={600}>drift {Math.round(tl.wEnd)} plf</text>
            }
          </g>
        );
      })}

      {/* Point loads — stacked */}
      {plRender.map((pl) => {
        const px = margin + pl.d * scale;
        const tipY = by - 2;
        const topY = tipY - pl.arrowH;
        return (
          <g key={`pl${pl.idx}`}>
            <line x1={px} y1={topY} x2={px} y2={tipY} stroke="#f43f5e" strokeWidth={2} markerEnd="url(#arrowRed)" />
            <text x={px} y={topY - 4} textAnchor="middle" fill="#f43f5e" fontSize="9" fontWeight={600}>{pl.P} lb</text>
          </g>
        );
      })}

      {/* Span label */}
      <text x={margin + bw / 2} y={by + 16} textAnchor="middle" fill="#94a3b8" fontSize="10">{span} ft</text>

      <defs>
        {colors.map((c, i) => (
          <marker key={i} id={`arr${i}`} markerWidth="6" markerHeight="6" refX="3" refY="6" orient="auto">
            <path d={`M0,0 L3,6 L6,0`} fill="none" stroke={c} strokeWidth="1" />
          </marker>
        ))}
        <marker id="arrowRed" markerWidth="6" markerHeight="6" refX="3" refY="6" orient="auto">
          <path d="M0,0 L3,6 L6,0" fill="none" stroke="#f43f5e" strokeWidth="1" />
        </marker>
        <marker id="arrowCyan" markerWidth="5" markerHeight="5" refX="2.5" refY="5" orient="auto">
          <path d="M0,0 L2.5,5 L5,0" fill="none" stroke="#7dd3fc" strokeWidth="0.8" />
        </marker>
      </defs>
    </svg>
  );
}

// ═══════════════════════════════════════════════════════════════════════
// CAUTION INPUT FIELD
// ═══════════════════════════════════════════════════════════════════════
function CautionInput({ label, value, defaultValue, onChange, unit, disabled }) {
  const isOverridden = defaultValue !== undefined && value !== defaultValue && !disabled;
  return (
    <div style={{ position: "relative", display: "flex", flexDirection: "column", gap: 2 }}>
      <label style={{ fontSize: 11, color: "#94a3b8", fontWeight: 500, display: "flex", alignItems: "center", gap: 4 }}>
        {label}
        {isOverridden && <span title="Manually overridden" style={{ fontSize: 14, cursor: "help" }}>⚠️</span>}
      </label>
      <div style={{ display: "flex", alignItems: "center" }}>
        <input
          type="number"
          value={value}
          onChange={e => onChange(parseFloat(e.target.value) || 0)}
          disabled={disabled}
          style={{
            width: "100%",
            padding: "6px 8px",
            background: isOverridden ? "#422006" : "#1e293b",
            border: isOverridden ? "1px solid #f59e0b" : "1px solid #334155",
            borderRadius: 6,
            color: "#f1f5f9",
            fontSize: 13,
            outline: "none",
            fontFamily: "'JetBrains Mono', monospace",
          }}
        />
        {unit && <span style={{ fontSize: 11, color: "#64748b", marginLeft: 4, whiteSpace: "nowrap" }}>{unit}</span>}
      </div>
    </div>
  );
}


// ═══════════════════════════════════════════════════════════════════════
// OWSJ REINFORCEMENT CHECK — TAB 3 (integrated from OWSJReinforcementCalc)
// ═══════════════════════════════════════════════════════════════════════

// ─── Engineering helpers ──────────────────────────────────────────────────────
const rodArea = (d) => Math.PI * d * d / 4;
const rodRx   = (d) => d / 4;

const ROD_SIZES = [
  { label: '1/4" ROD',  dia: 0.25  },
  { label: '3/8" ROD',  dia: 0.375 },
  { label: '1/2" ROD',  dia: 0.5   },
  { label: '5/8" ROD',  dia: 0.625 },
  { label: '3/4" ROD',  dia: 0.75  },
];

function computeResults(inputs) {
  const {
    span, depth,
    RE, dM,
    fp,
    deflLimit, Iunreinf, deflUnreinf,
    l1, l2, braced,
    webDia, webFy, webE, webFu, weldSize, weldLen, U,
    chordDia, chordFy, chordFu, chordE, chordWeldSpacing, chordU,
  } = inputs;

  const OMEGA_T = 1.67;
  const OMEGA_T2 = 2.00;   // for rupture term Fu·U·Ag / 2.00
  const OMEGA_C = 1.67;

  // ── SHEAR ───────────────────────────────────────────────────────────────────
  const shearReqd = RE > 0;

  const phi      = 90 - Math.atan(l1 / depth) * 180 / Math.PI;
  const alpha    = Math.atan(2 * depth / l2) * 180 / Math.PI;
  const phiRad   = phi   * Math.PI / 180;
  const alphaRad = alpha * Math.PI / 180;

  const TE = shearReqd ? RE / Math.sin(phiRad) : 0;
  const CE = shearReqd ? TE * Math.sin(phiRad) / Math.sin(alphaRad) : 0;

  const webAg     = rodArea(webDia) * 2;   // 2 members, one each side
  const FyAdj     = webFy - fp;

  // Tension: Pn/Ωt = min( F'y·Ag / 1.67 ,  Fu·U·Ag / 2.00 )
  const webTensYield   = FyAdj * webAg / OMEGA_T;
  const webTensRupture = webFu * U * webAg / OMEGA_T2;
  const webTensCap     = Math.min(webTensYield, webTensRupture);

  // Weld: 0.6 · 60ksi(electrode) · 0.707 · weld_size · weld_length · 2
  const weldCapacity = 0.6 * 60 * 0.707 * weldSize * weldLen * 2;

  // Compression — end web member
  const memberLen  = (l2 / 2) / Math.cos(alphaRad);
  const rx         = rodRx(webDia);
  const KL         = braced ? memberLen / 2 : memberLen;
  const KLr        = KL / rx;
  const Fe         = Math.PI * Math.PI * webE / (KLr * KLr);
  const Fcr        = Fe >= 0.44 * webFy
    ? webFy * Math.pow(0.658, webFy / Fe)
    : 0.877 * Fe;
  const webCompCap = Fcr * webAg / OMEGA_C;

  const webTensOK  = !shearReqd || webTensCap  >= TE;
  const weldTensOK = !shearReqd || weldCapacity >= TE;
  const webCompOK  = !shearReqd || webCompCap   >= CE;
  const weldCompOK = !shearReqd || weldCapacity >= CE;

  // ── MOMENT ──────────────────────────────────────────────────────────────────
  const momentReqd = dM > 0;
  const Deff       = depth / 12;
  const dP         = momentReqd ? dM / Deff : 0;

  const chordAg    = rodArea(chordDia) * 2;
  const chordRxVal = rodRx(chordDia);
  const chordKLr   = chordWeldSpacing / chordRxVal;
  const chordFe    = Math.PI * Math.PI * chordE / (chordKLr * chordKLr);
  const chordFcr   = chordFe >= 0.44 * chordFy
    ? chordFy * Math.pow(0.658, chordFy / chordFe)
    : 0.877 * chordFe;

  // Chord tension: min( Fy·Ag/1.67 ,  Fu·U·Ag/2.00 )
  const chordTensYield   = chordFy * chordAg / OMEGA_T;
  const chordTensRupture = chordFu * chordU * chordAg / OMEGA_T2;
  const chordTensCap     = Math.min(chordTensYield, chordTensRupture);
  const chordCompCap     = chordFcr * chordAg / OMEGA_C;

  const chordTensOK = !momentReqd || chordTensCap >= Math.abs(dP);
  const chordCompOK = !momentReqd || chordCompCap >= Math.abs(dP);

  // ── DEFLECTION ──────────────────────────────────────────────────────────────
  // I_added = 2 * ( 2*(π·d⁴/64) + Ag*(D/2 - 0.5 - d/2)² )
  //   d  = chord rod diameter (in)
  //   Ag = area of 2 rods (in²)
  //   D  = joist depth (in)
  //   0.5 = chord vertical offset (in)
  const Irod_self  = Math.PI * Math.pow(chordDia, 4) / 64;        // one rod self I
  const e          = depth / 2 - 0.5 - chordDia / 2;              // centroid offset from NA
  const Iadded     = 2 * (2 * Irod_self + chordAg * e * e);       // both chords (top+bottom)
  const Ireinf     = Iunreinf + Iadded;

  const deflReinf     = deflUnreinf * Iunreinf / Ireinf;
  const deflAllowable = (span * 12) / deflLimit;
  const deflOK        = deflReinf <= deflAllowable;
  const LoverDeflU    = Math.round((span * 12) / deflUnreinf);
  const LoverDeflR    = Math.round((span * 12) / deflReinf);

  const allOK = webTensOK && weldTensOK && webCompOK && weldCompOK
              && chordTensOK && chordCompOK
              && deflOK;

  return {
    RE, shearReqd,
    phi, alpha, TE, CE,
    webAg, FyAdj,
    webTensYield, webTensRupture, webTensCap, weldCapacity,
    memberLen, rx, KLr, Fe, Fcr, webCompCap,
    webTensOK, weldTensOK, webCompOK, weldCompOK,
    dM, dP, momentReqd,
    chordAg, chordRxVal, chordKLr, chordFe, chordFcr,
    chordTensYield, chordTensRupture, chordTensCap, chordCompCap,
    chordTensOK, chordCompOK,
    deflReinf, deflAllowable, deflOK, LoverDeflU, LoverDeflR,
    Iadded, Ireinf,
    allOK,
  };
}

// ─── Design tokens ────────────────────────────────────────────────────────────
const TR = {
  bg0: "#0f172a",
  bg1: "#1e293b",
  bg2: "#273449",
  bg3: "#334155",
  border: "#334155",
  borderHi: "#475569",
  text0: "#e2e8f0",
  text1: "#94a3b8",
  text2: "#64748b",
  blue: "#38bdf8",
  teal: "#38bdf8",
  amber: "#f59e0b",
  red: "#ef4444",
  green: "#10b981",
  purple: "#a78bfa",
  mono: "'JetBrains Mono', 'IBM Plex Mono', monospace",
};

const sR = {
  page:        { fontFamily: "'IBM Plex Sans', 'Segoe UI', sans-serif", background: TR.bg0, minHeight: "100vh", color: TR.text0 },
  header:      { background: TR.bg1, borderBottom: `1px solid ${TR.border}`, padding: "18px 28px 14px", position: "sticky", top: 0, zIndex: 10 },
  hTitle:      { fontSize: 11, fontWeight: 700, letterSpacing: "0.18em", color: TR.teal, textTransform: "uppercase", margin: 0 },
  hSub:        { fontSize: 10, color: TR.text2, letterSpacing: "0.08em", marginTop: 3 },
  main:        { padding: "20px 24px 40px", maxWidth: 960, margin: "0 auto" },

  // Demand row — full width at top
  demandBox:   { background: TR.bg1, border: `1px solid ${TR.amber}66`, borderRadius: 10, padding: "16px", marginBottom: 16 },
  demandTitle: { fontSize: 11, fontWeight: 700, letterSpacing: "0.05em", color: TR.amber, textTransform: "uppercase", marginBottom: 12, display: "flex", alignItems: "center", gap: 7 },
  demandDot:   { width: 6, height: 6, borderRadius: "50%", background: TR.amber, flexShrink: 0 },
  demandNote:  { fontSize: 11, color: TR.text2, marginTop: 8, lineHeight: 1.7, borderTop: `1px solid ${TR.border}`, paddingTop: 8 },
  demandRow:   { display: "grid", gridTemplateColumns: "1fr 1fr", gap: 12 },

  // Module = full-width card with inputs on left, results panel on right
  module:      { background: TR.bg1, border: `1px solid ${TR.border}`, borderRadius: 10, marginBottom: 16, overflow: "hidden" },
  moduleHead:  (c) => ({ background: TR.bg2, borderBottom: `1px solid ${TR.border}`, padding: "10px 16px", display: "flex", alignItems: "center", gap: 8, fontSize: 11, fontWeight: 700, letterSpacing: "0.05em", color: c, textTransform: "uppercase" }),
  moduleDot:   (c) => ({ width: 6, height: 6, borderRadius: "50%", background: c, flexShrink: 0 }),
  moduleBody:  { display: "grid", gridTemplateColumns: "320px 1fr", minHeight: 0 },
  moduleLeft:  { padding: "16px", borderRight: `1px solid ${TR.border}` },
  moduleRight: { padding: 0 },

  // Input fields
  fRow:        { display: "grid", gridTemplateColumns: "1fr 1fr", gap: 12, marginBottom: 10 },
  fRow3:       { display: "grid", gridTemplateColumns: "1fr 1fr 1fr", gap: 12, marginBottom: 10 },
  fRowFull:    { marginBottom: 10 },
  field:       { display: "flex", flexDirection: "column", gap: 3 },
  flabel:      { fontSize: 11, color: TR.text1, fontWeight: 500 },
  fnote:       { fontSize: 10, color: TR.text2, lineHeight: 1.4, marginTop: 1 },
  input:       { background: TR.bg1, border: `1px solid ${TR.border}`, borderRadius: 6, color: "#f1f5f9", fontSize: 13, padding: "6px 8px", fontFamily: TR.mono, outline: "none", width: "100%", boxSizing: "border-box" },
  select:      { background: TR.bg1, border: `1px solid ${TR.border}`, borderRadius: 6, color: "#f1f5f9", fontSize: 13, padding: "6px 8px", fontFamily: TR.mono, outline: "none", cursor: "pointer", width: "100%", boxSizing: "border-box" },

  // Results within module
  rSubHead:    (c) => ({ padding: "7px 14px 5px", fontSize: 11, color: c || TR.text1, letterSpacing: "0.05em", textTransform: "uppercase", fontWeight: 700, borderBottom: `1px solid ${TR.border}` }),
  rRow:        { display: "flex", justifyContent: "space-between", alignItems: "center", padding: "6px 14px", borderBottom: `1px solid ${TR.bg3}`, fontSize: 13 },
  rRowHL:      { display: "flex", justifyContent: "space-between", alignItems: "center", padding: "6px 14px", borderBottom: `1px solid ${TR.bg3}`, fontSize: 13, background: TR.bg2 },
  rLabel:      { color: TR.text1 },
  rVal:        { color: TR.text0, fontWeight: 600, fontFamily: TR.mono, display: "flex", alignItems: "center", gap: 8 },
  rNote:       { fontSize: 10, color: TR.text2, marginTop: 1 },
  mGrid:       { display: "grid", gridTemplateColumns: "1fr 1fr", gap: 1, borderBottom: `1px solid ${TR.border}` },
  mCell:       { background: TR.bg2, padding: "10px 14px" },
  mLabel:      { fontSize: 10, color: TR.text2, letterSpacing: "0.08em", textTransform: "uppercase", marginBottom: 3 },
  mVal:        { fontSize: 16, fontWeight: 700, color: TR.text0, fontFamily: TR.mono },
  mSub:        { fontSize: 11, color: TR.text1, marginTop: 2 },
  divider:     { border: "none", borderTop: `1px solid ${TR.border}`, margin: "2px 0" },

  badge:       (ok, na) => ({
    fontSize: 11, fontWeight: 700, padding: "2px 8px", borderRadius: 4, letterSpacing: "0.04em",
    background: na ? TR.bg3 : ok ? "#065f46" : "#7f1d1d",
    color:      na ? TR.text2 : ok ? "#6ee7b7" : "#fca5a5",
    border:     `1px solid ${na ? TR.border : ok ? "#10b981" : "#ef4444"}`,
  }),
  govBadge:    { fontSize: 11, fontWeight: 700, padding: "2px 8px", borderRadius: 4, letterSpacing: "0.04em", background: "rgba(167,139,250,0.12)", color: TR.purple, border: "1px solid rgba(167,139,250,0.35)" },

  // Run button
  btnWrap:     { marginTop: 16 },
  btn:         (hov) => ({ width: "100%", padding: "10px 0", background: hov ? "rgba(56,189,248,0.08)" : "transparent", border: `1px solid ${hov ? TR.blue : TR.borderHi}`, borderRadius: 6, color: TR.blue, fontSize: 12, fontWeight: 700, letterSpacing: "0.05em", textTransform: "uppercase", cursor: "pointer", fontFamily: "'IBM Plex Sans', sans-serif", transition: "all 0.15s" }),

  bannerOK:    { background: "#065f46", border: "1px solid #10b981", borderRadius: 8, padding: "12px 16px", fontSize: 13, color: "#6ee7b7", marginTop: 4, fontWeight: 700 },
  bannerNG:    { background: "#7f1d1d", border: "1px solid #ef4444", borderRadius: 8, padding: "12px 16px", fontSize: 13, color: "#fca5a5", marginTop: 4, fontWeight: 700 },
  footer:      { fontSize: 10, color: TR.text2, marginTop: 20, lineHeight: 1.8 },
};

// ─── Small components ─────────────────────────────────────────────────────────
function F({ label, note, children }) {
  return (
    <div style={sR.field}>
      <span style={sR.flabel}>{label}</span>
      {children}
      {note && <span style={sR.fnote}>{note}</span>}
    </div>
  );
}

function NI({ value, onChange, step = 0.001, min }) {
  return (
    <input type="number" style={sR.input} value={value} step={step} min={min}
      onChange={e => onChange(parseFloat(e.target.value) || 0)} />
  );
}

function RS({ value, onChange }) {
  return (
    <select style={sR.select} value={value} onChange={e => onChange(parseFloat(e.target.value))}>
      {ROD_SIZES.map(r => <option key={r.dia} value={r.dia}>{r.label}</option>)}
    </select>
  );
}

// Read-only locked display
function LI({ value }) {
  return (
    <div style={{
      background: TR.bg0, border: `1px solid ${TR.border}`, borderRadius: 6,
      color: TR.text1, fontSize: 13, padding: "6px 8px", fontFamily: TR.mono,
      minHeight: 28, display: "flex", alignItems: "center",
    }}>{value}</div>
  );
}

// Edit / Lock toggle button for module headers
function EditBtn({ locked, onToggle }) {
  return (
    <button onClick={onToggle} style={{
      marginLeft: "auto",
      background: "transparent",
      border: `1px solid ${locked ? TR.borderHi : TR.amber}`,
      borderRadius: 6,
      color: locked ? TR.text2 : TR.amber,
      fontSize: 11, fontWeight: 600, letterSpacing: "0.04em",
      textTransform: "uppercase", fontFamily: "'IBM Plex Sans', sans-serif",
      padding: "2px 10px", cursor: "pointer",
    }}>{locked ? "Edit" : "Lock"}</button>
  );
}

function Badge({ ok, na }) {
  return <span style={sR.badge(ok, na)}>{na ? "N/A" : ok ? "O.K." : "N.G."}</span>;
}

function RRow({ label, value, ok, na, highlight, governs }) {
  return (
    <div style={highlight ? sR.rRowHL : sR.rRow}>
      <span style={sR.rLabel}>{label}</span>
      <span style={sR.rVal}>
        <span>{value}</span>
        {governs && <span style={sR.govBadge}>governs</span>}
        {(ok !== undefined || na) && <Badge ok={ok} na={na} />}
      </span>
    </div>
  );
}

function MPair({ a, b }) {
  return (
    <div style={sR.mGrid}>
      {[a, b].map((m, i) => (
        <div key={i} style={sR.mCell}>
          <div style={sR.mLabel}>{m.label}</div>
          <div style={sR.mVal}>{m.value}</div>
          {m.sub && <div style={sR.mSub}>{m.sub}</div>}
        </div>
      ))}
    </div>
  );
}

function ModuleHead({ color, children, editBtn }) {
  return (
    <div style={{ ...sR.moduleHead(color), justifyContent: "space-between" }}>
      <div style={{ display: "flex", alignItems: "center", gap: 8 }}>
        <span style={sR.moduleDot(color)} />
        {children}
      </div>
      {editBtn}
    </div>
  );
}

// ─── Joist web geometry diagram ──────────────────────────────────────────────
function WebDiagram({ depth, l1, l2, phi, alpha }) {
  // Canvas
  const W = 360, H = 200;
  // Margins: left for DEPTH label, top for chord clearance, bottom for dim arrows
  const ml = 56, mr = 10, mt = 10, mb = 46;
  const dw = W - ml - mr;
  const dh = H - mt - mb;

  // Horizontal scale: show l1 + 3*l2 so we get the full warren context
  const totalSpan = l1 + 3 * l2;
  const sx = dw / totalSpan;

  // Key x coordinates (all in SVG pixels)
  const xA = ml;                   // Node A — top-left, start of T_E
  const xB = ml + l1 * sx;        // Node B — bottom-left, end of T_E / start of C_E
  const xC = xB + l2 * sx;        // Node C — top, end of C_E
  const xD = xC + l2 * sx;        // Node D — bottom
  const xE = xD + l2 * sx;        // Node E — top (right edge)

  // y coordinates
  const yT = mt;        // top chord
  const yB = mt + dh;   // bottom chord

  // ── Angles computed from pixel geometry — used only for arc drawing ──
  const phiRad   = Math.atan2(yB - yT, xB - xA);
  const alphaDraw = Math.atan2(yB - yT, xC - xB);

  // Display values come from the engineering calculation (matches results panel)
  const phiDeg   = phi.toFixed(1);
  const alphaDeg = alpha.toFixed(1);

  // ── Dashed reference line ──
  // In the reference image a dashed line goes from a point on the top chord
  // (roughly above where T_E's midpoint is) down to node B, showing angle construction.
  // It represents the "second adjacent web" going top→B from x midpoint of l1.
  const xMid = xA + (xB - xA) * 0.5;  // midpoint of l1 along top chord
  // dash goes from (xMid, yT) down to B
  const dashX1 = xMid, dashY1 = yT;
  const dashX2 = xB,   dashY2 = yB;

  const DIM = "#475569";
  const LBL = "#94a3b8";
  const ANG = TR.teal;
  const CH  = TR.text0;
  const RE  = TR.amber;
  const EX  = "#4a5568";

  // Arrowhead helper: draw a simple triangle at end of dimension line
  // We just use polyline + small triangles rather than SVG markers (avoids refX issues)
  const arr = (x, y, dir) => {
    // dir: "R","L","U","D"
    const s = 5;
    const pts = {
      R: `${x},${y-s*0.5} ${x+s},${y} ${x},${y+s*0.5}`,
      L: `${x},${y-s*0.5} ${x-s},${y} ${x},${y+s*0.5}`,
      U: `${x-s*0.5},${y} ${x},${y-s} ${x+s*0.5},${y}`,
      D: `${x-s*0.5},${y} ${x},${y+s} ${x+s*0.5},${y}`,
    }[dir];
    return <polygon points={pts} fill={DIM} />;
  };

  return (
    <div style={{ marginTop: 14, borderTop: `1px solid ${TR.border}`, paddingTop: 12 }}>
      <div style={{ fontSize: 9, color: TR.text2, letterSpacing: "0.09em", textTransform: "uppercase", marginBottom: 4 }}>
        Joist web member layout
      </div>
      <svg width="100%" viewBox={`0 0 ${W} ${H}`} style={{ display: "block" }}>

        {/* ══ CHORDS ══ */}
        {/* Top chord — full width, thick */}
        <line x1={xA} y1={yT} x2={W - mr} y2={yT} stroke={CH} strokeWidth="4" strokeLinecap="square" />
        {/* Bottom chord — starts at B */}
        <line x1={xB - 1} y1={yB} x2={W - mr} y2={yB} stroke={CH} strokeWidth="4" strokeLinecap="square" />

        {/* ══ DASHED REFERENCE LINE ══ */}
        {/* From midpoint of l1 on top chord diagonally to node B */}
        <line x1={dashX1} y1={dashY1} x2={dashX2} y2={dashY2}
          stroke={LBL} strokeWidth="1.4" strokeDasharray="6 4" />

        {/* ══ HIGHLIGHTED WEB MEMBERS (T_E and C_E) ══ */}
        <line x1={xA} y1={yT} x2={xB} y2={yB} stroke={RE} strokeWidth="2.8" strokeLinecap="round" />
        <line x1={xB} y1={yB} x2={xC} y2={yT} stroke={RE} strokeWidth="2.8" strokeLinecap="round" />

        {/* ══ ADDITIONAL WARREN PANELS (context) ══ */}
        <line x1={xC} y1={yT} x2={xD} y2={yB} stroke={EX} strokeWidth="2" strokeLinecap="round" />
        <line x1={xD} y1={yB} x2={xE} y2={yT} stroke={EX} strokeWidth="2" strokeLinecap="round" />

        {/* ══ NODE DOT at B ══ */}
        <circle cx={xB} cy={yB} r="4" fill={RE} />

        {/* ══ φ ARC at A ══
            φ = angle from HORIZONTAL (top chord, rightward) down to T_E
            Arc sweeps clockwise from (xA+r, yT) to the T_E direction */}
        {(() => {
          const r = 28;
          // Start point: horizontal right along top chord from A
          const p1x = xA + r;
          const p1y = yT;
          // End point: along T_E unit vector scaled by r
          const len  = Math.sqrt((xB - xA) ** 2 + (yB - yT) ** 2);
          const p2x  = xA + r * (xB - xA) / len;
          const p2y  = yT + r * (yB - yT) / len;
          // φ from horizontal = atan2(dy, dx) of T_E
          const phiFromHoriz = Math.atan2(yB - yT, xB - xA);
          // Label at arc bisector (halfway between horizontal and T_E direction)
          const bisR = phiFromHoriz / 2;
          const lx   = xA + (r + 12) * Math.cos(bisR);
          const ly   = yT + (r +  4) * Math.sin(bisR);
          return (
            <>
              <path d={`M ${p1x} ${p1y} A ${r} ${r} 0 0 1 ${p2x} ${p2y}`}
                fill="none" stroke={ANG} strokeWidth="1.5" />
              <text x={lx} y={ly} textAnchor="middle" fontSize="11"
                fill={ANG} fontFamily={TR.mono} fontWeight="700">φ</text>
              <text x={lx + 14} y={ly} textAnchor="start" fontSize="8"
                fill={ANG} fontFamily={TR.mono}>{phiDeg}°</text>
            </>
          );
        })()}

        {/* ══ α ARC at B ══
            α = angle from HORIZONTAL (right) to C_E
            Arc sweeps counter-clockwise from (xB+r, yB) up to C_E direction */}
        {(() => {
          const r = 26;
          // Start: horizontal right
          const p1x = xB + r;
          const p1y = yB;
          // End: along C_E unit vector from B
          const len  = Math.sqrt((xC-xB)**2 + (yT-yB)**2);
          const p2x  = xB + r * (xC-xB) / len;
          const p2y  = yB + r * (yT-yB) / len;  // yT-yB is negative → goes up
          // Label at bisector
          const bisR = alphaDraw / 2;
          const lx   = xB + (r + 10) * Math.cos(bisR);
          const ly   = yB - (r +  4) * Math.sin(bisR);
          return (
            <>
              {/* horizontal reference tick */}
              <line x1={xB} y1={yB} x2={xB + r + 4} y2={yB}
                stroke={ANG} strokeWidth="0.8" strokeDasharray="3 2" opacity="0.5" />
              <path d={`M ${p1x} ${p1y} A ${r} ${r} 0 0 0 ${p2x} ${p2y}`}
                fill="none" stroke={ANG} strokeWidth="1.5" />
              <text x={lx} y={ly} textAnchor="middle" fontSize="11"
                fill={ANG} fontFamily={TR.mono} fontWeight="700">α</text>
              <text x={lx + 14} y={ly} textAnchor="start" fontSize="8"
                fill={ANG} fontFamily={TR.mono}>{alphaDeg}°</text>
            </>
          );
        })()}

        {/* ══ MEMBER LABELS ══ */}
        {/* T_E — positioned left of and below midpoint of the member */}
        <text x={(xA + xB) / 2 - 16} y={(yT + yB) / 2 + 6}
          fontSize="10" fill={RE} fontFamily={TR.mono} fontWeight="700">T_E</text>
        {/* C_E — positioned right of and above midpoint */}
        <text x={(xB + xC) / 2 + 8} y={(yT + yB) / 2 - 4}
          fontSize="10" fill={RE} fontFamily={TR.mono} fontWeight="700">C_E</text>

        {/* ══ DEPTH DIMENSION (left side) ══ */}
        {(() => {
          const dx = ml - 26;
          return (
            <>
              <line x1={dx} y1={yT + 5} x2={dx} y2={yB - 5} stroke={DIM} strokeWidth="1" />
              {arr(dx, yT + 5, "U")}
              {arr(dx, yB - 5, "D")}
              <line x1={dx - 6} y1={yT} x2={dx + 6} y2={yT} stroke={DIM} strokeWidth="0.8" />
              <line x1={dx - 6} y1={yB} x2={dx + 6} y2={yB} stroke={DIM} strokeWidth="0.8" />
              <text x={dx - 8} y={(yT + yB) / 2 + 3} textAnchor="middle" fontSize="9"
                fill={LBL} fontFamily={TR.mono}
                transform={`rotate(-90,${dx - 8},${(yT + yB) / 2})`}>
                d = {depth} in
              </text>
            </>
          );
        })()}

        {/* ══ l₁ DIMENSION (bottom) ══ */}
        {(() => {
          const dy = yB + 20;
          return (
            <>
              <line x1={xA + 5} y1={dy} x2={xB - 5} y2={dy} stroke={DIM} strokeWidth="1" />
              {arr(xA + 5, dy, "L")}
              {arr(xB - 5, dy, "R")}
              <line x1={xA} y1={yB + 13} x2={xA} y2={yB + 27} stroke={DIM} strokeWidth="0.8" />
              <line x1={xB} y1={yB + 13} x2={xB} y2={yB + 27} stroke={DIM} strokeWidth="0.8" />
              <text x={(xA + xB) / 2} y={yB + 40} textAnchor="middle" fontSize="10"
                fill={LBL} fontFamily={TR.mono}>l₁ = {l1} in</text>
            </>
          );
        })()}

        {/* ══ l₂ DIMENSION (bottom) ══ */}
        {(() => {
          const dy = yB + 20;
          return (
            <>
              <line x1={xB + 5} y1={dy} x2={xC - 5} y2={dy} stroke={DIM} strokeWidth="1" />
              {arr(xB + 5, dy, "L")}
              {arr(xC - 5, dy, "R")}
              <line x1={xC} y1={yB + 13} x2={xC} y2={yB + 27} stroke={DIM} strokeWidth="0.8" />
              <text x={(xB + xC) / 2} y={yB + 40} textAnchor="middle" fontSize="10"
                fill={LBL} fontFamily={TR.mono}>l₂ = {l2} in</text>
            </>
          );
        })()}

        {/* ══ DEPTH value on left (vertical label) ══ */}

      </svg>
    </div>
  );
}

// ─── Defaults ─────────────────────────────────────────────────────────────────
const DEFAULTS = {
  span: 30.67, depth: 26,
  RE: 0.162,
  dM: 0,
  fp: 0,
  deflLimit: 240, Iunreinf: 125.59, deflUnreinf: 1.613,
  l1: 36, l2: 24, braced: true,
  webDia: 0.375, webFy: 36, webE: 29000, webFu: 58,
  weldSize: 0.125, weldLen: 1.0, U: 0.625,
  chordDia: 0.5, chordFy: 36, chordFu: 58, chordE: 29000, chordWeldSpacing: 12, chordU: 1.0,
};

// ─── App ──────────────────────────────────────────────────────────────────────
export function OWSJReinforcementCalc({
  span        = 30.67,
  depth       = 26,
  RE          = 0,
  dM          = 0,
  Iunreinf    = 0,
  deflUnreinf = 0,
  // Lifted state — owned by parent so it survives tab switches
  inp, setInp,
  webLocked, setWebLocked,
  chordLocked, setChordLocked,
  scheduleZones = null,
}) {
  useEffect(() => {
    setInp(prev => ({ ...prev, span, depth, RE, dM, Iunreinf, deflUnreinf }));
  }, [span, depth, RE, dM, Iunreinf, deflUnreinf]);

  const R = useMemo(() => computeResults(inp), [inp]);

  const set = useCallback((k) => (v) => setInp(p => ({ ...p, [k]: v })), []);

  const f   = (v, d = 3) => typeof v === "number" ? v.toFixed(d) : "—";
  const fki = (v, d = 3) => f(v, d) + " kips";
  const fks = (v, d = 2) => f(v, d) + " ksi";
  const fi2 = (v)        => f(v, 4) + " in²";
  const fin = (v, d = 3) => f(v, d) + " in";
  const fdg = (v)        => f(v, 1) + "°";

  const yieldGoverns   = R.webTensYield   <= R.webTensRupture;
  const ruptureGoverns = R.webTensRupture <  R.webTensYield;

  const chYieldGoverns   = R.chordTensYield   <= R.chordTensRupture;
  const chRuptureGoverns = R.chordTensRupture <  R.chordTensYield;

  return (
    <div style={sR.main}>

        {/* ── DEMAND ── */}
        <div style={sR.demandBox}>
          <div style={sR.demandTitle}>
            <span style={sR.demandDot} />
            Required reinforcement demand
          </div>
          <div style={sR.demandRow}>
            <F label="Add'l shear force req'd RE (kips)" note="From Tab 2 joist analysis">
              <LI value={typeof inp.RE === 'number' ? inp.RE.toFixed(3) : '0.000'} />
            </F>
            <F label="Add'l moment req'd ΔM (k·ft)" note="From Tab 2 joist analysis">
              <LI value={typeof inp.dM === 'number' ? inp.dM.toFixed(3) : '0.000'} />
            </F>
          </div>
          <div style={sR.demandNote}>
            RE and ΔM come from an independent shear/moment analysis of the existing joist under total new load.
          </div>
        </div>

        {/* ── MODULE 1: JOIST GEOMETRY ── */}
        <div style={sR.module}>
          <ModuleHead color={TR.blue}>Joist geometry</ModuleHead>
          <div style={{ padding: "14px 18px" }}>
            <div style={sR.fRow}>
              <F label="Span L (ft)" note="From Tab 1 joist selection"><LI value={typeof inp.span === 'number' ? inp.span.toFixed(2) : inp.span} /></F>
              <F label="Depth d (in)" note="From joist designation"><LI value={typeof inp.depth === 'number' ? inp.depth.toFixed(0) : inp.depth} /></F>
              <F label="Prestress f_p (ksi)" note="In-place dead load stress"><NI value={inp.fp} onChange={set("fp")} step={0.1} /></F>
            </div>
          </div>
        </div>

        {/* ── MODULE 2: WEB REINFORCING (SHEAR) ── */}
        <div style={sR.module}>
          <ModuleHead color={TR.amber} editBtn={<EditBtn locked={webLocked} onToggle={() => setWebLocked(l => !l)} />}>
            Web reinforcing — shear check
          </ModuleHead>
          <div style={sR.moduleBody}>

            {/* Inputs */}
            <div style={sR.moduleLeft}>
              <div style={sR.fRow}>
                <F label="Rod size (ea. side)"><RS value={inp.webDia} onChange={set("webDia")} /></F>
                <F label="F_y (ksi)">{webLocked ? <LI value={inp.webFy} /> : <NI value={inp.webFy} onChange={set("webFy")} step={1} />}</F>
              </div>
              <div style={sR.fRow}>
                <F label="E (ksi)">{webLocked ? <LI value={inp.webE} /> : <NI value={inp.webE} onChange={set("webE")} step={1000} />}</F>
                <F label="F_u (ksi)">{webLocked ? <LI value={inp.webFu} /> : <NI value={inp.webFu} onChange={set("webFu")} step={1} />}</F>
              </div>
              <div style={sR.fRow}>
                <F label="Weld size (in)">{webLocked ? <LI value={inp.weldSize} /> : <NI value={inp.weldSize} onChange={set("weldSize")} step={0.0625} />}</F>
                <F label="Weld length (in/end)">{webLocked ? <LI value={inp.weldLen} /> : <NI value={inp.weldLen} onChange={set("weldLen")} step={0.25} />}</F>
              </div>
              <div style={sR.fRow}>
                <F label="Shear lag U">{webLocked ? <LI value={inp.U} /> : <NI value={inp.U} onChange={set("U")} step={0.025} />}</F>
                <F label="l₁ near panel (in)">{webLocked ? <LI value={inp.l1} /> : <NI value={inp.l1} onChange={set("l1")} step={0.5} />}</F>
              </div>
              <div style={sR.fRow}>
                <F label="l₂ far panel (in)">{webLocked ? <LI value={inp.l2} /> : <NI value={inp.l2} onChange={set("l2")} step={0.5} />}</F>
                <F label="Braced mid-pt?">
                  {webLocked
                    ? <LI value={inp.braced ? "Yes — KL = L/2" : "No  — KL = L"} />
                    : <select style={sR.select} value={inp.braced ? "Y" : "N"}
                        onChange={e => setInp(p => ({ ...p, braced: e.target.value === "Y" }))}>
                        <option value="Y">Yes — KL = L/2</option>
                        <option value="N">No  — KL = L</option>
                      </select>
                  }
                </F>
              </div>

              {/* Joist web geometry diagram */}
              <WebDiagram depth={inp.depth} l1={inp.l1} l2={inp.l2} phi={R.phi} alpha={R.alpha} />
            </div>

            {/* Results */}
            <div style={sR.moduleRight}>
              <MPair
                a={{ label: "Req'd shear RE", value: R.shearReqd ? fki(R.RE) : "0.000 kips", sub: R.shearReqd ? "" : "no web reinf. req'd" }}
                b={{ label: "Web angle φ / α", value: fdg(R.phi) + " / " + fdg(R.alpha) }}
              />
              <MPair
                a={{ label: "Tension force TE", value: R.shearReqd ? fki(R.TE) : "—" }}
                b={{ label: "Comp. force CE",   value: R.shearReqd ? fki(R.CE) : "—" }}
              />

              <div style={sR.rSubHead(TR.amber)}>Tension check</div>
              <RRow label="Rod area Ag (2 members)"            value={fi2(R.webAg)} />
              <RRow label="F′y = Fy − fp"                      value={fks(R.FyAdj)} />
              <RRow label="Yield  →  F′y·Ag / 1.67"
                value={fki(R.webTensYield)}
                governs={yieldGoverns}
                highlight={yieldGoverns} />
              <RRow label="Rupture  →  Fu·U·Ag / 2.00"
                value={fki(R.webTensRupture)}
                governs={ruptureGoverns}
                highlight={ruptureGoverns} />
              <RRow label="Tension capacity Pn/Ωt  (min)"
                value={fki(R.webTensCap)}
                ok={R.webTensOK}
                na={!R.shearReqd} />
              <RRow label="Weld capacity  0.6·60·0.707·a·L·2"
                value={fki(R.weldCapacity)}
                ok={R.weldTensOK}
                na={!R.shearReqd} />

              <hr style={sR.divider} />
              <div style={sR.rSubHead(TR.amber)}>Compression check</div>
              <RRow label="Member length"          value={fin(R.memberLen)} />
              <RRow label="rx"                     value={f(R.rx, 4) + " in"} />
              <RRow label="KL/r"                   value={f(R.KLr, 1)} />
              <RRow label="Fe"                     value={fks(R.Fe)} />
              <RRow label="Fcr"                    value={fks(R.Fcr)} />
              <RRow label="Comp. capacity Pn/Ωc"   value={fki(R.webCompCap)}   ok={R.webCompOK}  na={!R.shearReqd} />
              <RRow label="Weld capacity (comp.)"  value={fki(R.weldCapacity)} ok={R.weldCompOK} na={!R.shearReqd} />
            </div>
          </div>
        </div>

        {/* ── MODULE 3: CHORD REINFORCING (MOMENT) ── */}
        <div style={sR.module}>
          <ModuleHead color={TR.red} editBtn={<EditBtn locked={chordLocked} onToggle={() => setChordLocked(l => !l)} />}>
            Chord reinforcing — moment check
          </ModuleHead>

          {/* Inputs */}
          <div style={{ padding: "14px 18px", borderBottom: `1px solid ${TR.border}` }}>
            <div style={{ display: "grid", gridTemplateColumns: "repeat(3, 1fr)", gap: 10 }}>
              <F label="Rod size (2T&amp;B ea. side)"><RS value={inp.chordDia} onChange={set("chordDia")} /></F>
              <F label="F_y (ksi)">{chordLocked ? <LI value={inp.chordFy} /> : <NI value={inp.chordFy} onChange={set("chordFy")} step={1} />}</F>
              <F label="F_u (ksi)">{chordLocked ? <LI value={inp.chordFu} /> : <NI value={inp.chordFu} onChange={set("chordFu")} step={1} />}</F>
              <F label="Shear lag U">{chordLocked ? <LI value={inp.chordU} /> : <NI value={inp.chordU} onChange={set("chordU")} step={0.025} />}</F>
              <F label="E (ksi)">{chordLocked ? <LI value={inp.chordE} /> : <NI value={inp.chordE} onChange={set("chordE")} step={1000} />}</F>
              <F label="Weld spacing (in)" note="Used as KL for compression">
                {chordLocked ? <LI value={inp.chordWeldSpacing} /> : <NI value={inp.chordWeldSpacing} onChange={set("chordWeldSpacing")} step={1} />}
              </F>
            </div>
          </div>

          {/* Results — full-width */}
          <div style={sR.moduleRight}>
              <MPair
                a={{ label: "Add'l moment ΔM",  value: R.momentReqd ? f(R.dM, 3) + " k·ft" : "0.000 k·ft", sub: R.momentReqd ? "" : "no chord reinf. req'd" }}
                b={{ label: "Add'l chord force", value: R.momentReqd ? fki(Math.abs(R.dP)) : "—" }}
              />

              <div style={sR.rSubHead(TR.red)}>Tension check</div>
              <RRow label="Ag per chord (2 rods)"           value={fi2(R.chordAg)} />
              <RRow label="rx"                              value={f(R.chordRxVal, 4) + " in"} />
              <RRow label="Yield  →  Fy·Ag / 1.67"
                value={fki(R.chordTensYield)}
                governs={chYieldGoverns}
                highlight={chYieldGoverns} />
              <RRow label="Rupture  →  Fu·U·Ag / 2.00"
                value={fki(R.chordTensRupture)}
                governs={chRuptureGoverns}
                highlight={chRuptureGoverns} />
              <RRow label="Tension capacity Pn/Ωt  (min)"
                value={fki(R.chordTensCap)}
                ok={R.chordTensOK}
                na={!R.momentReqd} />

              <hr style={sR.divider} />
              <div style={sR.rSubHead(TR.red)}>Compression check</div>
              <RRow label="KL/r"                            value={f(R.chordKLr, 1)} />
              <RRow label="Fe"                              value={fks(R.chordFe)} />
              <RRow label="Fcr"                             value={fks(R.chordFcr)} />
              <RRow label="Compression capacity Pn/Ωc"     value={fki(R.chordCompCap)} ok={R.chordCompOK} na={!R.momentReqd} />
            </div>
          {/* end chord module */}
        </div>

        {/* ── MODULE 4: DEFLECTION ── */}
        <div style={sR.module}>
          <ModuleHead color={TR.teal}>Deflection — total load</ModuleHead>
          <div style={sR.moduleBody}>

            {/* Inputs */}
            <div style={sR.moduleLeft}>
              <div style={sR.fRow}>
                <F label="I_unreinf (in⁴)" note="From Tab 1 joist analysis">
                  <LI value={typeof inp.Iunreinf === 'number' ? inp.Iunreinf.toFixed(3) : '0.000'} />
                </F>
                <F label="Defl. limit L/">
                  <NI value={inp.deflLimit} onChange={set("deflLimit")} step={10} min={1} />
                </F>
              </div>
              <div style={sR.fRowFull}>
                <F label="Defl_unreinf (in)" note="From Tab 2 joist analysis (total load)">
                  <LI value={typeof inp.deflUnreinf === 'number' ? inp.deflUnreinf.toFixed(3) : '0.000'} />
                </F>
              </div>
            </div>

            {/* Results */}
            <div style={sR.moduleRight}>
              <MPair
                a={{ label: "Unreinforced", value: fin(inp.deflUnreinf), sub: "L/" + R.LoverDeflU }}
                b={{ label: "Reinforced",   value: fin(R.deflReinf),     sub: "L/" + R.LoverDeflR }}
              />
              <div style={sR.rSubHead(TR.teal)}>Moment of inertia</div>
              <RRow label="I_unreinf"                        value={f(inp.Iunreinf, 3) + " in⁴"} />
              <RRow label="I_added  (chord rods, top+bot)"   value={f(R.Iadded, 3) + " in⁴"} />
              <RRow label="I_reinf  = I_unreinf + I_added"   value={f(R.Ireinf, 3) + " in⁴"} />
              <hr style={sR.divider} />
              <RRow
                label={"Allowable  (L/" + inp.deflLimit + ")"}
                value={fin(R.deflAllowable)}
                ok={R.deflOK}
              />
            </div>
          </div>
        </div>

        {/* Summary banner */}
        <div style={R.allOK ? sR.bannerOK : sR.bannerNG}>
          {R.allOK
            ? "✓  All checks pass — reinforcing scheme is adequate."
            : "✗  One or more checks fail — revise rod size, weld geometry, or reinforcing scheme."}
        </div>

        {/* ── REINFORCEMENT SCHEDULE TABLE ── */}
        {(() => {
          const ms = scheduleZones ? scheduleZones.momentSchedule : null;
          const vs = scheduleZones ? scheduleZones.shearSchedule  : null;
          const spanLabel = scheduleZones ? scheduleZones.span : '—';

          // Rod size labels from ROD_SIZES lookup
          const webRodLabel  = ROD_SIZES.find(r => r.dia === inp.webDia)?.label  || `${inp.webDia}" DIA. ROD`;
          const chordRodLabel= ROD_SIZES.find(r => r.dia === inp.chordDia)?.label|| `${inp.chordDia}" DIA. ROD`;

          const cellBase = {
            padding: '8px 12px',
            textAlign: 'center',
            fontSize: 12,
            fontFamily: TR.mono,
            color: TR.text0,
            borderRight: `1px solid #1a3a2a`,
            borderBottom: `1px solid #1a3a2a`,
          };
          const headerCell = {
            ...cellBase,
            fontSize: 10,
            fontWeight: 700,
            letterSpacing: '0.12em',
            color: '#00ff88',
            textTransform: 'uppercase',
            background: '#0a1f14',
            paddingTop: 10,
            paddingBottom: 10,
          };
          const subHeaderCell = {
            ...headerCell,
            fontSize: 10,
            letterSpacing: '0.10em',
            color: '#00cc66',
          };
          const dataCell = {
            ...cellBase,
            fontSize: 13,
            fontWeight: 600,
            color: '#00ff88',
            background: '#061209',
          };
          const rodCell = {
            ...cellBase,
            fontSize: 11,
            fontWeight: 700,
            color: '#00cc66',
            background: '#0a1f14',
            letterSpacing: '0.06em',
          };
          const spanCell = {
            ...cellBase,
            fontSize: 13,
            fontWeight: 700,
            color: '#00ff88',
            background: '#061209',
            borderRight: 'none',
          };
          const tableWrap = {
            marginTop: 20,
            border: '2px solid #00aa55',
            borderRadius: 6,
            overflow: 'hidden',
            fontFamily: "'IBM Plex Sans', sans-serif",
          };
          const titleRow = {
            background: '#061209',
            borderBottom: '2px solid #00aa55',
            textAlign: 'center',
            padding: '10px 0 8px',
            fontSize: 14,
            fontWeight: 700,
            letterSpacing: '0.22em',
            color: '#00ff88',
            textTransform: 'uppercase',
          };

          return (
            <div style={tableWrap}>
              <div style={titleRow}>Joist Reinforcement Schedule</div>
              <table style={{ width: '100%', borderCollapse: 'collapse', background: '#061209' }}>
                <colgroup>
                  <col style={{ width: '14%' }} />
                  <col style={{ width: '10%' }} />
                  <col style={{ width: '10%' }} />
                  <col style={{ width: '10%' }} />
                  <col style={{ width: '14%' }} />
                  <col style={{ width: '10%' }} />
                  <col style={{ width: '10%' }} />
                  <col style={{ width: '10%' }} />
                  <col style={{ width: '12%' }} />
                </colgroup>
                <thead>
                  {/* Row 1: group headers */}
                  <tr>
                    <th rowSpan={2} style={{ ...headerCell, verticalAlign: 'middle', borderRight: '2px solid #00aa55' }}>
                      CHORD<br />REINF.
                    </th>
                    <th colSpan={3} style={{ ...headerCell, borderBottom: `1px solid #1a3a2a`, borderRight: '2px solid #00aa55' }}>
                      CHORD REINF.
                    </th>
                    <th rowSpan={2} style={{ ...headerCell, verticalAlign: 'middle', borderRight: '2px solid #00aa55' }}>
                      WEB<br />REINF.
                    </th>
                    <th colSpan={3} style={{ ...headerCell, borderBottom: `1px solid #1a3a2a`, borderRight: '2px solid #00aa55' }}>
                      WEB REINF.
                    </th>
                    <th rowSpan={2} style={{ ...headerCell, verticalAlign: 'middle', borderRight: 'none' }}>
                      SPAN
                    </th>
                  </tr>
                  {/* Row 2: sub-labels A B C / D E F */}
                  <tr>
                    <th style={{ ...subHeaderCell, borderRight: `1px solid #1a3a2a` }}>"A"</th>
                    <th style={{ ...subHeaderCell, borderRight: `1px solid #1a3a2a` }}>"B"</th>
                    <th style={{ ...subHeaderCell, borderRight: '2px solid #00aa55' }}>"C"</th>
                    <th style={{ ...subHeaderCell, borderRight: `1px solid #1a3a2a` }}>"D"</th>
                    <th style={{ ...subHeaderCell, borderRight: `1px solid #1a3a2a` }}>"E"</th>
                    <th style={{ ...subHeaderCell, borderRight: '2px solid #00aa55' }}>"F"</th>
                  </tr>
                </thead>
                <tbody>
                  <tr>
                    {/* Chord rod size */}
                    <td style={{ ...rodCell, borderRight: '2px solid #00aa55' }}>
                      {chordRodLabel.replace(' ROD', '').trim()}<br />
                      <span style={{ fontSize: 10, color: '#00aa55', letterSpacing: '0.08em' }}>DIA. ROD</span>
                    </td>
                    {/* A B C */}
                    <td style={{ ...dataCell }}>{ms ? ms.A : '—'}</td>
                    <td style={{ ...dataCell }}>{ms ? ms.B : '—'}</td>
                    <td style={{ ...dataCell, borderRight: '2px solid #00aa55' }}>{ms ? `±${ms.C}` : '—'}</td>
                    {/* Web rod size */}
                    <td style={{ ...rodCell, borderRight: '2px solid #00aa55' }}>
                      {webRodLabel.replace(' ROD', '').trim()}<br />
                      <span style={{ fontSize: 10, color: '#00aa55', letterSpacing: '0.08em' }}>DIA. ROD</span>
                    </td>
                    {/* D E F */}
                    <td style={{ ...dataCell }}>{vs ? vs.D : '—'}</td>
                    <td style={{ ...dataCell }}>{vs ? vs.E : '—'}</td>
                    <td style={{ ...dataCell, borderRight: '2px solid #00aa55' }}>{vs ? `±${vs.F}` : '—'}</td>
                    {/* Span */}
                    <td style={spanCell}>±{spanLabel}</td>
                  </tr>
                </tbody>
              </table>
              <div style={{ background: '#061209', borderTop: `1px solid #1a3a2a`, padding: '6px 14px', fontSize: 10, color: '#00aa55', letterSpacing: '0.06em' }}>
                "A","B","C" from moment exceedance zones · "D","E","F" from shear exceedance zones · All dimensions rounded up to nearest 0′-6″ · "—" indicates no exceedance
              </div>
            </div>
          );
        })()}

        <p style={sR.footer}>
          AISC 360-05 ASD · Ω_t = 1.67 (yield) / 2.00 (rupture) · Ω_c = 1.67 ·
          Tension capacity = min(F′y·Ag/1.67, Fu·U·Ag/2.00) · "governs" tag marks controlling term ·
          Web compression: circular rod, no angle slenderness correction ·
          Weld: 0.6 · 60ksi (E70 electrode) × 0.707 × a × L × 2 ·
          I_reinf must include chord rod contribution (parallel-axis from rod centroid to joist N.A.)
        </p>
    </div>
  );
}


// ═══════════════════════════════════════════════════════════════════════
// MAIN APP
// ═══════════════════════════════════════════════════════════════════════
export default function BarJoistCalculator() {
  const [activeTab, setActiveTab] = useState("tab1");
  const [projectName, setProjectName] = useState("Untitled Project");
  const fileInputRef = useRef(null);

  // Joist selection
  const [joistId, setJoistId] = useState("");
  const [span, setSpan] = useState("");

  const joistIds = useMemo(() => Object.keys(JOIST_DB).sort((a, b) => {
    const [da, na] = [parseInt(a), parseInt(a.split("K")[1])];
    const [db, nb] = [parseInt(b), parseInt(b.split("K")[1])];
    return da - db || na - nb;
  }), []);

  const availableSpans = useMemo(() => {
    if (!joistId || !JOIST_DB[joistId]) return [];
    return JOIST_DB[joistId].map(r => r[0]);
  }, [joistId]);

  const joistRecord = useMemo(() => {
    if (!joistId || !span || !JOIST_DB[joistId]) return null;
    const s = parseInt(span);
    const entry = JOIST_DB[joistId].find(r => r[0] === s);
    if (!entry) return null;
    return { total_load_plf: entry[1], live_load_l360_plf: entry[2] };
  }, [joistId, span]);

  // Tab 1 computed defaults
  const defaultLiveLoad = joistRecord ? joistRecord.live_load_l360_plf : 0;
  const defaultDeadLoad = joistRecord ? joistRecord.total_load_plf - joistRecord.live_load_l360_plf : 0;
  const spanNum = parseFloat(span) || 0;
  // Parse depth from joist designation (e.g. "26K7" → 26 in)
  const joistDepthIn = joistId ? parseInt(joistId.split("K")[0]) || 0 : 0;
  const defaultE = 29000;

  const [liveLoad, setLiveLoad] = useState(0);
  const [deadLoad, setDeadLoad] = useState(0);
  const [Eval, setEval] = useState(29000);
  const [Ival, setIval] = useState(0);

  // I is derived from current liveLoad (W in the SJI formula)
  const computedI = spanNum > 0 ? 26.767 * liveLoad * Math.pow(spanNum - 0.33, 3) * 1e-6 : 0;
  const defaultI = Math.round(computedI * 1000) / 1000;

  // Track overrides
  const [llOverridden, setLlOverridden] = useState(false);
  const [dlOverridden, setDlOverridden] = useState(false);
  const [eOverridden, setEOverridden] = useState(false);
  const [iOverridden, setIOverridden] = useState(false);

  // Auto-populate when joist record changes
  useEffect(() => {
    if (joistRecord) {
      if (!llOverridden) setLiveLoad(joistRecord.live_load_l360_plf);
      if (!dlOverridden) setDeadLoad(joistRecord.total_load_plf - joistRecord.live_load_l360_plf);
      if (!eOverridden) setEval(29000);
    }
  }, [joistRecord, spanNum]);

  // Keep I in sync with liveLoad unless user has directly overridden I
  useEffect(() => {
    if (!iOverridden && spanNum > 0) {
      const calcI = 26.767 * liveLoad * Math.pow(spanNum - 0.33, 3) * 1e-6;
      setIval(Math.round(calcI * 1000) / 1000);
    }
  }, [liveLoad, spanNum, iOverridden]);

  // Reset overrides when joist changes
  useEffect(() => {
    setLlOverridden(false);
    setDlOverridden(false);
    setEOverridden(false);
    setIOverridden(false);
  }, [joistId, span]);

  // Tab 1: Mechanical zone additional loads
  const [mechUL, setMechUL] = useState({ w: 0, a: 0, b: 0 });
  const [mechPL, setMechPL] = useState({ P: 0, d: 0 });

  // Tab 1 analysis
  const tab1Results = useMemo(() => {
    if (!spanNum || !Eval || !Ival) return null;
    const totalLoad = liveLoad + deadLoad;
    if (totalLoad <= 0 && mechUL.w <= 0 && mechPL.P <= 0) return null;

    const allUL = [];
    if (totalLoad > 0) allUL.push({ w: totalLoad, a: 0, b: spanNum });
    if (mechUL.w > 0 && mechUL.b > mechUL.a) allUL.push({ w: mechUL.w, a: mechUL.a, b: mechUL.b });

    const allPL = [];
    if (mechPL.P > 0) allPL.push(mechPL);

    return analyzeBeam(spanNum, allUL, allPL, Eval, Ival);
  }, [spanNum, liveLoad, deadLoad, Eval, Ival, mechUL, mechPL]);

  // Capacity Envelope: allowable moment and shear envelopes per SJI
  const capacityEnvelope = useMemo(() => {
    if (!tab1Results || !spanNum) return null;
    const L = spanNum;
    const n = NUM_POINTS;
    const dx = L / n;
    const xArr = Array.from({ length: n + 1 }, (_, i) => i * dx);

    const Mallow = tab1Results.maxM; // wL^2/8 from the analysis
    const Vmax = tab1Results.maxV;   // wL/2 from the analysis
    const Vcap = 0.25 * Vmax;        // mid-span shear capacity

    // Moment envelope
    const Menv = xArr.map(x => {
      if (x <= 4) return (Mallow / 4) * x;
      if (x >= L - 4) return (Mallow / 4) * (L - x);
      return Mallow;
    });

    // Shear envelope
    const slopeRate = (Vmax - Vcap) / (0.375 * L);
    const Venv = xArr.map(x => {
      if (x <= 0.375 * L) {
        return Vmax - slopeRate * x;
      } else if (x < 0.5 * L) {
        return Vcap;
      } else if (x <= 0.5 * L + dx * 0.5) {
        // Step at midspan — use negative side
        return -Vcap;
      } else if (x <= 0.625 * L) {
        return -Vcap;
      } else {
        return -Vcap - slopeRate * (x - 0.625 * L);
      }
    });

    return { x: xArr, M: Menv, V: Venv, maxM: Mallow, maxV: Vmax, Vcap };
  }, [tab1Results, spanNum]);

  // Tab 2: Roof load (full span, psf with spacing)
  const [roofDL_psf, setRoofDL_psf] = useState(0);
  const [roofLL_psf, setRoofLL_psf] = useState(0);
  const [joistSpacing, setJoistSpacing] = useState(0);
  const [roofLoadType, setRoofLoadType] = useState("Live Load"); // "Live Load" or "Snow Load"

  // Snow drift (triangular): left and right sides
  const [snowDriftL_psf, setSnowDriftL_psf] = useState(0);
  const [snowDriftLStart, setSnowDriftLStart] = useState(0);
  const [snowDriftLEnd, setSnowDriftLEnd] = useState(0);
  const [snowDriftR_psf, setSnowDriftR_psf] = useState(0);
  const [snowDriftRStart, setSnowDriftRStart] = useState(0);
  const [snowDriftREnd, setSnowDriftREnd] = useState(0);

  // Derived plf values
  const roofDL_plf = roofDL_psf * joistSpacing;
  const roofLL_plf = roofLL_psf * joistSpacing;
  const roofTotal_plf = roofDL_plf + roofLL_plf;
  const snowDriftL_plf = snowDriftL_psf * joistSpacing;
  const snowDriftR_plf = snowDriftR_psf * joistSpacing;

  // Tab 2: Additional uniform loads with load type
  const [uniformLoads2, setUniformLoads2] = useState([]);
  // Tab 2: Point loads with load type
  const [pointLoads2, setPointLoads2] = useState([]);

  const updateUL = (idx, field, val) => {
    const copy = [...uniformLoads2];
    copy[idx] = { ...copy[idx], [field]: val };
    setUniformLoads2(copy);
  };
  const updatePL = (idx, field, val) => {
    const copy = [...pointLoads2];
    copy[idx] = { ...copy[idx], [field]: val };
    setPointLoads2(copy);
  };
  const addUL = () => setUniformLoads2([...uniformLoads2, { w: 0, a: 0, b: spanNum, type: "Dead Load" }]);
  const addPL = () => setPointLoads2([...pointLoads2, { P: 0, d: spanNum / 2, type: "Dead Load", name: `Point Load ${pointLoads2.length + 1}` }]);
  const removeUL = (i) => setUniformLoads2(uniformLoads2.filter((_, j) => j !== i));
  const removePL = (i) => setPointLoads2(pointLoads2.filter((_, j) => j !== i));

  // ─── Save / Open Project ───
  const getProjectData = () => ({
    version: 1,
    projectName,
    activeTab,
    tab1: {
      joistId, span,
      liveLoad, deadLoad, Eval, Ival,
      llOverridden, dlOverridden, eOverridden, iOverridden,
      mechUL, mechPL,
    },
    tab2: {
      roofDL_psf, roofLL_psf, joistSpacing, roofLoadType,
      snowDriftL_psf, snowDriftLStart, snowDriftLEnd,
      snowDriftR_psf, snowDriftRStart, snowDriftREnd,
      uniformLoads2,
      pointLoads2,
    },
  });

  const saveProject = () => {
    const data = JSON.stringify(getProjectData(), null, 2);
    const blob = new Blob([data], { type: "application/json" });
    const url = URL.createObjectURL(blob);
    const a = document.createElement("a");
    a.href = url;
    a.download = `${projectName || "Untitled Project"}.bjc`;
    document.body.appendChild(a);
    a.click();
    document.body.removeChild(a);
    URL.revokeObjectURL(url);
  };

  const openProject = (e) => {
    const file = e.target.files?.[0];
    if (!file) return;
    const reader = new FileReader();
    reader.onload = (ev) => {
      try {
        const d = JSON.parse(ev.target.result);
        if (d.projectName !== undefined) setProjectName(d.projectName);
        if (d.activeTab) setActiveTab(d.activeTab);
        // Tab 1
        if (d.tab1) {
          const t = d.tab1;
          if (t.joistId !== undefined) setJoistId(t.joistId);
          if (t.span !== undefined) setSpan(t.span);
          // Defer setting overridden values so they don't get clobbered by auto-populate
          setTimeout(() => {
            if (t.liveLoad !== undefined) setLiveLoad(t.liveLoad);
            if (t.deadLoad !== undefined) setDeadLoad(t.deadLoad);
            if (t.Eval !== undefined) setEval(t.Eval);
            if (t.Ival !== undefined) setIval(t.Ival);
            if (t.llOverridden !== undefined) setLlOverridden(t.llOverridden);
            if (t.dlOverridden !== undefined) setDlOverridden(t.dlOverridden);
            if (t.eOverridden !== undefined) setEOverridden(t.eOverridden);
            if (t.iOverridden !== undefined) setIOverridden(t.iOverridden);
            if (t.mechUL) setMechUL(t.mechUL);
            if (t.mechPL) setMechPL(t.mechPL);
          }, 100);
        }
        // Tab 2
        if (d.tab2) {
          const t = d.tab2;
          if (t.roofDL_psf !== undefined) setRoofDL_psf(t.roofDL_psf);
          if (t.roofLL_psf !== undefined) setRoofLL_psf(t.roofLL_psf);
          if (t.joistSpacing !== undefined) setJoistSpacing(t.joistSpacing);
          if (t.roofLoadType) setRoofLoadType(t.roofLoadType);
          if (t.snowDriftL_psf !== undefined) setSnowDriftL_psf(t.snowDriftL_psf);
          if (t.snowDriftLStart !== undefined) setSnowDriftLStart(t.snowDriftLStart);
          if (t.snowDriftLEnd !== undefined) setSnowDriftLEnd(t.snowDriftLEnd);
          if (t.snowDriftR_psf !== undefined) setSnowDriftR_psf(t.snowDriftR_psf);
          if (t.snowDriftRStart !== undefined) setSnowDriftRStart(t.snowDriftRStart);
          if (t.snowDriftREnd !== undefined) setSnowDriftREnd(t.snowDriftREnd);
          // Legacy single drift support
          if (t.snowDrift_psf !== undefined && t.snowDriftL_psf === undefined) {
            setSnowDriftL_psf(t.snowDrift_psf);
            if (t.snowDriftStart !== undefined) setSnowDriftLStart(t.snowDriftStart);
            if (t.snowDriftEnd !== undefined) setSnowDriftLEnd(t.snowDriftEnd);
          }
          if (t.uniformLoads2) setUniformLoads2(t.uniformLoads2);
          if (t.pointLoads2) setPointLoads2(t.pointLoads2);
        }
      } catch (err) {
        alert("Could not read project file. Make sure it is a valid .bjc file.");
      }
    };
    reader.readAsText(file);
    // Reset input so same file can be re-opened
    e.target.value = "";
  };

  // Tab 2: Analyze using ONLY Tab 2 loads (ASD: D + L)
  const tab2Results = useMemo(() => {
    if (!spanNum || !Eval || !Ival) return null;

    // Build all uniform loads for analysis
    const allUL = [];
    if (roofTotal_plf > 0) {
      allUL.push({ w: roofTotal_plf, a: 0, b: spanNum });
    }
    for (const u of uniformLoads2) {
      if (u.w !== 0 && u.b > u.a) {
        allUL.push({ w: u.w, a: u.a, b: u.b });
      }
    }

    // Build all point loads for analysis
    const allPL = pointLoads2.filter(p => p.P !== 0);

    // Build triangular loads (snow drift — left and right)
    const allTri = [];
    if (roofLoadType === "Snow Load") {
      if (snowDriftL_plf > 0 && snowDriftLEnd > snowDriftLStart) {
        allTri.push({ wStart: snowDriftL_plf, wEnd: 0, a: snowDriftLStart, b: snowDriftLEnd });
      }
      if (snowDriftR_plf > 0 && snowDriftREnd > snowDriftRStart) {
        // Right drift: peak at end (right side), taper to 0 at start
        allTri.push({ wStart: 0, wEnd: snowDriftR_plf, a: snowDriftRStart, b: snowDriftREnd });
      }
    }

    if (allUL.length === 0 && allPL.length === 0 && allTri.length === 0) return null;
    return analyzeBeam(spanNum, allUL, allPL, Eval, Ival, allTri);
  }, [spanNum, Eval, Ival, roofTotal_plf, uniformLoads2, pointLoads2, roofLoadType, snowDriftL_plf, snowDriftLStart, snowDriftLEnd, snowDriftR_plf, snowDriftRStart, snowDriftREnd]);

  // Comparison
  const comparison = useMemo(() => {
    if (!tab1Results || !tab2Results) return null;
    const env = capacityEnvelope;

    // Compute max deviation along the span, returning worst-point values
    const computeMaxDeviation = (demandArr, capArr, demandX, capX) => {
      let maxPct = 0;
      let maxDev = 0;
      let worstDemand = 0;
      let worstCap = 0;
      const step = Math.max(1, Math.floor(demandArr.length / 500));
      for (let i = 0; i < demandArr.length; i += step) {
        const xi = demandX[i];
        const dVal = Math.abs(demandArr[i]);
        let cVal = 0;
        const idx = capX.findIndex(cx => cx >= xi);
        if (idx <= 0) cVal = Math.abs(capArr[0] || 0);
        else {
          const t = (xi - capX[idx-1]) / (capX[idx] - capX[idx-1] || 1);
          cVal = Math.abs(capArr[idx-1] + t * (capArr[idx] - capArr[idx-1]));
        }
        if (cVal > 0 && dVal > cVal) {
          const pct = ((dVal - cVal) / cVal) * 100;
          const dev = dVal - cVal;
          if (dev > maxDev) {
            maxPct = pct;
            maxDev = dev;
            worstDemand = dVal;
            worstCap = cVal;
          }
        }
      }
      return { maxPct, maxDev, worstDemand, worstCap };
    };

    // Use envelope for moment and shear, actual analysis for deflection
    const mDev = env
      ? computeMaxDeviation(tab2Results.M, env.M, tab2Results.x, env.x)
      : computeMaxDeviation(tab2Results.M, tab1Results.M, tab2Results.x, tab1Results.x);
    const vDev = env
      ? computeMaxDeviation(tab2Results.V, env.V, tab2Results.x, env.x)
      : computeMaxDeviation(tab2Results.V, tab1Results.V, tab2Results.x, tab1Results.x);
    const dDev = computeMaxDeviation(tab2Results.defl, tab1Results.defl, tab2Results.x, tab1Results.x);

    const mPass = mDev.maxPct < 0.1;
    const vPass = vDev.maxPct < 0.1;
    const dPass = dDev.maxPct < 0.1;

    return {
      moment: { cap: env ? env.maxM : tab1Results.maxM, demand: tab2Results.maxM, pass: mPass, devPct: mDev.maxPct, maxDev: mDev.maxDev, worstDemand: mDev.worstDemand, worstCap: mDev.worstCap },
      shear: { cap: env ? env.maxV : tab1Results.maxV, demand: tab2Results.maxV, pass: vPass, devPct: vDev.maxPct, maxDev: vDev.maxDev, worstDemand: vDev.worstDemand, worstCap: vDev.worstCap },
      deflection: { cap: tab1Results.maxDefl, demand: tab2Results.maxDefl, pass: dPass, devPct: dDev.maxPct, maxDev: dDev.maxDev, worstDemand: dDev.worstDemand, worstCap: dDev.worstCap },
      overall: mPass && vPass && dPass,
    };
  }, [tab1Results, tab2Results, capacityEnvelope]);

  // Values piped into Tab 3 (Reinforcement Check)
  const reinfProps = {
    span:        spanNum,
    depth:       joistDepthIn,
    RE:          comparison && !comparison.shear.pass   ? Math.round(comparison.shear.maxDev)   / 1000 : 0,
    dM:          comparison && !comparison.moment.pass  ? Math.round(comparison.moment.maxDev)  / 1000 : 0,
    Iunreinf:    Ival,
    deflUnreinf: tab2Results ? tab2Results.maxDefl : 0,
  };

  // ─── Tab 3 lifted state (persists across tab switches) ───
  const [reinfInp, setReinfInp] = useState({
    ...DEFAULTS,
    span: 0, depth: 0, RE: 0, dM: 0, Iunreinf: 0, deflUnreinf: 0,
  });
  const [reinfWebLocked, setReinfWebLocked]     = useState(true);
  const [reinfChordLocked, setReinfChordLocked] = useState(true);

  // ─── Reinforcement Schedule: compute A/B/C (moment) and D/E/F (shear) ───
  // Helper: extract exceedance zones [{start, end}] from demand vs capacity arrays
  const getExceedanceZones = (demandArr, demandX, capArr, capX) => {
    const zones = [];
    if (!demandArr || !capArr || !demandX || !capX) return zones;
    let inExceed = false;
    let exceedStart = 0;
    const step = Math.max(1, Math.floor(demandArr.length / 500));
    for (let i = 0; i <= demandArr.length; i += (i < demandArr.length ? step : 0)) {
      if (i > demandArr.length - 1) i = demandArr.length - 1;
      const xi = demandX[i];
      const dVal = Math.abs(demandArr[i]);
      // interpolate cap at xi
      const idx = capX.findIndex(cx => cx >= xi);
      let cVal = 0;
      if (idx <= 0) cVal = Math.abs(capArr[0] || 0);
      else {
        const t = (xi - capX[idx - 1]) / (capX[idx] - capX[idx - 1] || 1);
        cVal = Math.abs(capArr[idx - 1] + t * (capArr[idx] - capArr[idx - 1]));
      }
      const exceeds = dVal > cVal * 1.001;
      if (exceeds && !inExceed) { exceedStart = xi; inExceed = true; }
      else if (!exceeds && inExceed) { zones.push({ start: exceedStart, end: xi }); inExceed = false; }
      if (i === demandArr.length - 1) {
        if (inExceed) zones.push({ start: exceedStart, end: xi });
        break;
      }
    }
    return zones;
  };

  // Round up to nearest 0.5 ft, format as ft-in string
  const roundUp05 = (v) => Math.ceil(v * 2) / 2;
  const fmtFtIn = (v) => {
    const r = roundUp05(v);
    const ft = Math.floor(r);
    const inches = Math.round((r - ft) * 12);
    return inches === 0 ? `${ft}'-0"` : `${ft}'-${inches}"`;
  };

  const scheduleZones = useMemo(() => {
    if (!tab2Results || !capacityEnvelope) return null;
    const span = spanNum;

    // Moment zones → A, B, C
    const mZones = getExceedanceZones(tab2Results.M, tab2Results.x, capacityEnvelope.M, capacityEnvelope.x);
    let momentSchedule = null;
    if (mZones.length > 0) {
      // Use the union of all moment exceedance zones (merge into single envelope)
      const zStart = Math.min(...mZones.map(z => z.start));
      const zEnd   = Math.max(...mZones.map(z => z.end));
      momentSchedule = {
        A: fmtFtIn(zStart),
        B: fmtFtIn(zEnd - zStart),
        C: fmtFtIn(span - zEnd),
        Araw: roundUp05(zStart),
        Braw: roundUp05(zEnd - zStart),
        Craw: roundUp05(span - zEnd),
      };
    }

    // Shear zones → D, E, F  (expect two zones: one near each end)
    const vZones = getExceedanceZones(tab2Results.V, tab2Results.x, capacityEnvelope.V, capacityEnvelope.x);
    let shearSchedule = null;
    if (vZones.length >= 2) {
      // Sort by start position
      const sorted = [...vZones].sort((a, b) => a.start - b.start);
      const z1 = sorted[0]; // near left end
      const z2 = sorted[sorted.length - 1]; // near right end
      shearSchedule = {
        D: fmtFtIn(z1.end),
        E: fmtFtIn(z2.start - z1.end),
        F: fmtFtIn(span - z2.start),
        Draw: roundUp05(z1.end),
        Eraw: roundUp05(z2.start - z1.end),
        Fraw: roundUp05(span - z2.start),
      };
    } else if (vZones.length === 1) {
      // Only one zone (asymmetric loading) — put in D
      const z1 = vZones[0];
      shearSchedule = {
        D: fmtFtIn(z1.end),
        E: '—',
        F: fmtFtIn(span - z1.end),
        Draw: roundUp05(z1.end),
        Eraw: null,
        Fraw: roundUp05(span - z1.end),
      };
    }

    return { momentSchedule, shearSchedule, span: fmtFtIn(span) };
  }, [tab2Results, capacityEnvelope, spanNum]);

  // ─── Styles ───
  const s = {
    app: {
      fontFamily: "'IBM Plex Sans', 'Segoe UI', sans-serif",
      background: "#0f172a",
      color: "#e2e8f0",
      minHeight: "100vh",
      padding: 0,
    },
    header: {
      background: "linear-gradient(135deg, #0f172a 0%, #1e293b 100%)",
      borderBottom: "1px solid #1e293b",
      padding: "16px 24px",
      display: "flex",
      alignItems: "center",
      gap: 12,
    },
    tabBar: {
      display: "flex",
      borderBottom: "1px solid #1e293b",
      background: "#0f172a",
    },
    tab: (active) => ({
      padding: "10px 20px",
      cursor: "pointer",
      fontSize: 13,
      fontWeight: 600,
      color: active ? "#38bdf8" : "#64748b",
      borderBottom: active ? "2px solid #38bdf8" : "2px solid transparent",
      background: "none",
      border: "none",
      borderBottomStyle: "solid",
      transition: "all 0.2s",
    }),
    panel: {
      padding: "20px 24px",
      maxWidth: 960,
      margin: "0 auto",
    },
    card: {
      background: "#1e293b",
      borderRadius: 10,
      border: "1px solid #334155",
      padding: 16,
      marginBottom: 16,
    },
    cardTitle: {
      fontSize: 13,
      fontWeight: 700,
      color: "#38bdf8",
      textTransform: "uppercase",
      letterSpacing: "0.05em",
      marginBottom: 12,
    },
    grid: (cols) => ({
      display: "grid",
      gridTemplateColumns: `repeat(${cols}, 1fr)`,
      gap: 12,
    }),
    select: {
      width: "100%",
      padding: "6px 8px",
      background: "#1e293b",
      border: "1px solid #334155",
      borderRadius: 6,
      color: "#f1f5f9",
      fontSize: 13,
      outline: "none",
    },
    resultBox: (highlight) => ({
      background: highlight ? "#7f1d1d22" : "#0f172a",
      border: highlight ? "1px solid #ef4444" : "1px solid #334155",
      borderRadius: 8,
      padding: "10px 14px",
      textAlign: "center",
    }),
    resultLabel: { fontSize: 10, color: "#94a3b8", textTransform: "uppercase", letterSpacing: "0.08em" },
    resultVal: { fontSize: 20, fontWeight: 700, fontFamily: "'JetBrains Mono', monospace" },
    btn: (variant) => ({
      padding: "6px 14px",
      borderRadius: 6,
      border: "none",
      fontSize: 12,
      fontWeight: 600,
      cursor: "pointer",
      background: variant === "danger" ? "#7f1d1d" : variant === "primary" ? "#1d4ed8" : "#334155",
      color: variant === "danger" ? "#fca5a5" : "#e2e8f0",
    }),
    badge: (pass) => ({
      display: "inline-block",
      padding: "4px 12px",
      borderRadius: 20,
      fontSize: 13,
      fontWeight: 700,
      letterSpacing: "0.04em",
      background: pass ? "#065f46" : "#7f1d1d",
      color: pass ? "#6ee7b7" : "#fca5a5",
      border: pass ? "1px solid #10b981" : "1px solid #ef4444",
    }),
  };

  const fmt = (v, dec = 1) => v !== undefined && v !== null ? v.toLocaleString("en-US", { minimumFractionDigits: dec, maximumFractionDigits: dec }) : "—";

  return (
    <div style={s.app}>
      <link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Sans:wght@400;500;600;700&family=JetBrains+Mono:wght@400;600;700&display=swap" rel="stylesheet" />

      {/* Hidden file input for Open */}
      <input
        ref={fileInputRef}
        type="file"
        accept=".bjc"
        style={{ display: "none" }}
        onChange={openProject}
      />

      {/* Header */}
      <div style={s.header}>
        <div style={{ width: 36, height: 36, borderRadius: 8, background: "linear-gradient(135deg, #3b82f6, #06b6d4)", display: "flex", alignItems: "center", justifyContent: "center", fontSize: 18, fontWeight: 800, color: "#fff", flexShrink: 0 }}>SJ</div>
        <div style={{ flexShrink: 0 }}>
          <div style={{ fontSize: 16, fontWeight: 700, color: "#f1f5f9" }}>Bar Joist Capacity Calculator</div>
          <div style={{ fontSize: 11, color: "#64748b" }}>SJI K-Series · ASD · Simply Supported</div>
        </div>

        {/* Project Name */}
        <div style={{ marginLeft: "auto", display: "flex", alignItems: "center", gap: 10 }}>
          <div style={{ display: "flex", flexDirection: "column", gap: 2 }}>
            <label style={{ fontSize: 9, color: "#475569", textTransform: "uppercase", letterSpacing: "0.08em" }}>Project Name</label>
            <input
              type="text"
              value={projectName}
              onChange={e => setProjectName(e.target.value)}
              onBlur={e => { if (!e.target.value.trim()) setProjectName("Untitled Project"); }}
              style={{
                padding: "5px 10px",
                background: "#0f172a",
                border: "1px solid #334155",
                borderRadius: 6,
                color: "#f1f5f9",
                fontSize: 13,
                fontWeight: 600,
                outline: "none",
                width: 200,
                fontFamily: "inherit",
              }}
              onFocus={e => e.target.style.borderColor = "#38bdf8"}
            />
          </div>

          <button
            onClick={() => fileInputRef.current?.click()}
            style={{ ...s.btn(), padding: "7px 14px", display: "flex", alignItems: "center", gap: 5, marginTop: 12 }}
            title="Open project file (.bjc)"
          >
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><path d="M22 19a2 2 0 0 1-2 2H4a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h5l2 3h9a2 2 0 0 1 2 2z"/></svg>
            Open
          </button>

          <button
            onClick={saveProject}
            style={{ ...s.btn("primary"), padding: "7px 14px", display: "flex", alignItems: "center", gap: 5, marginTop: 12 }}
            title="Save project file (.bjc)"
          >
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><path d="M19 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11l5 5v11a2 2 0 0 1-2 2z"/><polyline points="17 21 17 13 7 13 7 21"/><polyline points="7 3 7 8 15 8"/></svg>
            Save
          </button>
        </div>
      </div>

      {/* Tab Bar */}
      <div style={s.tabBar}>
        <button style={s.tab(activeTab === "tab1")} onClick={() => setActiveTab("tab1")}>① Existing Joist Capacity</button>
        <button style={s.tab(activeTab === "tab2")} onClick={() => setActiveTab("tab2")}>② Joist With Additional Load</button>
        <button style={s.tab(activeTab === "tab3")} onClick={() => setActiveTab("tab3")}>③ Bar Joist Reinforcement</button>
      </div>

      {/* ═══════════ TAB 1 ═══════════ */}
      {activeTab === "tab1" && (
        <div style={s.panel}>
          {/* Joist Selection */}
          <div style={s.card}>
            <div style={s.cardTitle}>Joist Selection</div>
            <div style={s.grid(2)}>
              <div>
                <label style={{ fontSize: 11, color: "#94a3b8", fontWeight: 500 }}>Joist Designation</label>
                <select
                  value={joistId}
                  onChange={e => { setJoistId(e.target.value); setSpan(""); }}
                  style={s.select}
                >
                  <option value="">Select joist…</option>
                  {joistIds.map(id => <option key={id} value={id}>{id}</option>)}
                </select>
              </div>
              <div>
                <label style={{ fontSize: 11, color: "#94a3b8", fontWeight: 500 }}>Span (ft)</label>
                <select
                  value={span}
                  onChange={e => setSpan(e.target.value)}
                  style={s.select}
                  disabled={!joistId}
                >
                  <option value="">Select span…</option>
                  {availableSpans.map(s => <option key={s} value={s}>{s} ft</option>)}
                </select>
              </div>
            </div>
          </div>

          {joistRecord && (
            <>
              {/* Properties */}
              <div style={s.card}>
                <div style={s.cardTitle}>Joist Properties & Loads (ASD: D + L)</div>
                <div style={s.grid(4)}>
                  <CautionInput label="Live Load" value={liveLoad} defaultValue={defaultLiveLoad} onChange={v => { setLiveLoad(v); setLlOverridden(v !== defaultLiveLoad); }} unit="plf" />
                  <CautionInput label="Dead Load" value={deadLoad} defaultValue={defaultDeadLoad} onChange={v => { setDeadLoad(v); setDlOverridden(v !== defaultDeadLoad); }} unit="plf" />
                  <CautionInput label="E (Elastic Mod.)" value={Eval} defaultValue={defaultE} onChange={v => { setEval(v); setEOverridden(v !== defaultE); }} unit="ksi" />
                  <CautionInput label="I (Moment of Inertia)" value={Ival} defaultValue={Math.round(defaultI * 1000) / 1000} onChange={v => { setIval(v); setIOverridden(true); }} unit="in⁴" />
                </div>
                <div style={{ marginTop: 10, fontSize: 11, color: "#475569" }}>
                  Total Load (D+L) = {fmt(liveLoad + deadLoad, 0)} plf &nbsp;|&nbsp; Span = {span} ft &nbsp;|&nbsp; Table Total = {joistRecord.total_load_plf} plf &nbsp;|&nbsp; Table LL (L/360) = {joistRecord.live_load_l360_plf} plf
                </div>
              </div>

              {/* Mechanical Zone Additional Load */}
              <div style={s.card}>
                <div style={s.cardTitle}>Mechanical Zone Additional Load <span style={{ fontWeight: 400, fontSize: 10, color: "#64748b" }}>(included in joist capacity)</span></div>

                <div style={{ marginBottom: 12 }}>
                  <div style={{ fontSize: 11, color: "#38bdf8", fontWeight: 600, marginBottom: 6 }}>Uniform Load</div>
                  <div style={s.grid(3)}>
                    <CautionInput label="Magnitude (plf)" value={mechUL.w} onChange={v => setMechUL(prev => ({ ...prev, w: v }))} unit="plf" />
                    <CautionInput label="Start" value={mechUL.a} onChange={v => setMechUL(prev => ({ ...prev, a: v }))} unit="ft" />
                    <CautionInput label="End" value={mechUL.b} onChange={v => setMechUL(prev => ({ ...prev, b: v }))} unit="ft" />
                  </div>
                </div>

                <div>
                  <div style={{ fontSize: 11, color: "#38bdf8", fontWeight: 600, marginBottom: 6 }}>Point Load</div>
                  <div style={s.grid(2)}>
                    <CautionInput label="Magnitude (lb)" value={mechPL.P} onChange={v => setMechPL(prev => ({ ...prev, P: v }))} unit="lb" />
                    <CautionInput label="Location" value={mechPL.d} onChange={v => setMechPL(prev => ({ ...prev, d: v }))} unit="ft" />
                  </div>
                </div>
              </div>

              {/* Beam Viz */}
              <div style={s.card}>
                <div style={s.cardTitle}>Structural Model</div>
                <BeamViz
                  span={spanNum}
                  uniformLoads={[
                    { w: liveLoad + deadLoad, a: 0, b: spanNum },
                    ...(mechUL.w > 0 && mechUL.b > mechUL.a ? [mechUL] : []),
                  ]}
                  pointLoads={mechPL.P > 0 ? [mechPL] : []}
                />
              </div>

              {/* Results */}
              {tab1Results && capacityEnvelope && (
                <>
                  <div style={s.card}>
                    <div style={s.cardTitle}>Capacity Results</div>
                    <div style={s.grid(4)}>
                      <div style={s.resultBox(false)}>
                        <div style={s.resultLabel}>Allowable Moment</div>
                        <div style={{ ...s.resultVal, color: "#38bdf8" }}>{fmt(capacityEnvelope.maxM, 0)}</div>
                        <div style={{ fontSize: 10, color: "#64748b" }}>lb·ft</div>
                      </div>
                      <div style={s.resultBox(false)}>
                        <div style={s.resultLabel}>Max Shear (End)</div>
                        <div style={{ ...s.resultVal, color: "#38bdf8" }}>{fmt(capacityEnvelope.maxV, 0)}</div>
                        <div style={{ fontSize: 10, color: "#64748b" }}>lb</div>
                      </div>
                      <div style={s.resultBox(false)}>
                        <div style={s.resultLabel}>Shear (Mid-span)</div>
                        <div style={{ ...s.resultVal, color: "#38bdf8" }}>{fmt(capacityEnvelope.Vcap, 0)}</div>
                        <div style={{ fontSize: 10, color: "#64748b" }}>lb</div>
                      </div>
                      <div style={s.resultBox(false)}>
                        <div style={s.resultLabel}>Max Deflection</div>
                        <div style={{ ...s.resultVal, color: "#38bdf8" }}>{fmt(tab1Results.maxDefl, 3)}</div>
                        <div style={{ fontSize: 10, color: "#64748b" }}>in</div>
                      </div>
                    </div>
                  </div>

                  <div style={s.card}>
                    <div style={s.cardTitle}>Allowable Capacity Envelopes</div>
                    <Diagram x={capacityEnvelope.x} y={capacityEnvelope.M} title="Moment Envelope" unit="lb·ft" color="#3b82f6" fillColor="rgba(59,130,246,0.10)" yTickInterval={5000} />
                    <Diagram x={capacityEnvelope.x} y={capacityEnvelope.V} title="Shear Envelope" unit="lb" color="#f59e0b" fillColor="rgba(245,158,11,0.10)" yTickInterval={1000} />
                    <Diagram x={tab1Results.x} y={tab1Results.defl} title="Deflection" unit="in" color="#06b6d4" fillColor="rgba(6,182,212,0.08)" />
                  </div>
                </>
              )}
            </>
          )}

          {!joistRecord && joistId && span && (
            <div style={{ ...s.card, borderColor: "#ef4444", color: "#fca5a5" }}>
              No data found for {joistId} at {span} ft span. Please select a valid span.
            </div>
          )}
        </div>
      )}

      {/* ═══════════ TAB 2 ═══════════ */}
      {activeTab === "tab2" && (
        <div style={s.panel}>
          {!joistRecord ? (
            <div style={{ ...s.card, borderColor: "#f59e0b", color: "#fcd34d" }}>
              Please select a joist and span in Tab 1 first.
            </div>
          ) : (
            <>
              {/* Joist reference */}
              <div style={{ ...s.card, background: "#0f172a" }}>
                <div style={{ fontSize: 12, color: "#64748b", marginBottom: 4 }}>Reference Joist (from Tab 1)</div>
                <span style={{ fontWeight: 700, color: "#38bdf8", fontSize: 15 }}>{joistId}</span>
                <span style={{ color: "#64748b", margin: "0 8px" }}>|</span>
                <span style={{ color: "#94a3b8" }}>Span: {span} ft</span>
                <span style={{ color: "#64748b", margin: "0 8px" }}>|</span>
                <span style={{ color: "#94a3b8" }}>E: {fmt(Eval, 0)} ksi</span>
                <span style={{ color: "#64748b", margin: "0 8px" }}>|</span>
                <span style={{ color: "#94a3b8" }}>I: {fmt(Ival, 3)} in⁴</span>
              </div>

              {/* Uniform Roof Load — full span, psf with spacing */}
              <div style={s.card}>
                <div style={s.cardTitle}>Uniform Roof Load <span style={{ fontWeight: 400, fontSize: 10, color: "#64748b" }}>(applied full span)</span></div>
                <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr 1fr 1fr 1fr", gap: 8, alignItems: "end" }}>
                  <CautionInput label="Dead Load" value={roofDL_psf} onChange={v => setRoofDL_psf(v)} unit="psf" />
                  <div style={{ display: "flex", flexDirection: "column", gap: 2 }}>
                    <label style={{ fontSize: 11, color: "#94a3b8", fontWeight: 500 }}>Load Type</label>
                    <select
                      value={roofLoadType}
                      onChange={e => setRoofLoadType(e.target.value)}
                      style={s.select}
                    >
                      <option value="Live Load">Live Load</option>
                      <option value="Snow Load">Snow Load</option>
                    </select>
                  </div>
                  <CautionInput label={roofLoadType === "Snow Load" ? "Balanced Snow (pf)" : "Live Load"} value={roofLL_psf} onChange={v => setRoofLL_psf(v)} unit="psf" />
                  <CautionInput label="Joist Spacing" value={joistSpacing} onChange={v => setJoistSpacing(v)} unit="ft" />
                  <div style={{ display: "flex", flexDirection: "column", justifyContent: "flex-end" }}>
                    <div style={{ fontSize: 11, color: "#94a3b8", fontWeight: 500, marginBottom: 2 }}>Total (D+{roofLoadType === "Snow Load" ? "S" : "L"})</div>
                    <div style={{
                      padding: "6px 8px",
                      background: "#0f172a",
                      border: "1px solid #334155",
                      borderRadius: 6,
                      color: "#38bdf8",
                      fontSize: 13,
                      fontFamily: "'JetBrains Mono', monospace",
                      fontWeight: 600,
                    }}>{roofTotal_plf.toLocaleString(undefined, { maximumFractionDigits: 1 })} plf</div>
                  </div>
                </div>
                <div style={{ marginTop: 8, fontSize: 11, color: "#475569" }}>
                  DL = {roofDL_plf.toLocaleString(undefined, { maximumFractionDigits: 1 })} plf &nbsp;|&nbsp; {roofLoadType === "Snow Load" ? "Balanced Snow" : "LL"} = {roofLL_plf.toLocaleString(undefined, { maximumFractionDigits: 1 })} plf &nbsp;|&nbsp; ({roofDL_psf} + {roofLL_psf}) psf × {joistSpacing} ft spacing
                </div>

                {/* Snow Drift — only shown when Snow Load selected */}
                {roofLoadType === "Snow Load" && (
                  <div style={{ marginTop: 14, display: "grid", gridTemplateColumns: "1fr 1fr", gap: 10 }}>
                    {/* Left Drift */}
                    <div style={{ padding: 12, background: "#0f172a", borderRadius: 8, border: "1px solid #1e3a5f" }}>
                      <div style={{ fontSize: 11, color: "#7dd3fc", fontWeight: 600, marginBottom: 8, display: "flex", alignItems: "center", gap: 6 }}>
                        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="#7dd3fc" strokeWidth="2"><polygon points="2,22 2,4 18,22" /></svg>
                        Left Snow Drift
                      </div>
                      <div style={{ display: "flex", flexDirection: "column", gap: 8 }}>
                        <CautionInput label="Peak Drift Load" value={snowDriftL_psf} onChange={v => setSnowDriftL_psf(v)} unit="psf" />
                        <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 8 }}>
                          <CautionInput label="Start (peak)" value={snowDriftLStart} onChange={v => setSnowDriftLStart(v)} unit="ft" />
                          <CautionInput label="End (tapers to 0)" value={snowDriftLEnd} onChange={v => setSnowDriftLEnd(v)} unit="ft" />
                        </div>
                      </div>
                      <div style={{ marginTop: 6, fontSize: 10, color: "#475569" }}>
                        {snowDriftL_plf.toLocaleString(undefined, { maximumFractionDigits: 1 })} plf peak at {snowDriftLStart} ft → 0 at {snowDriftLEnd} ft
                      </div>
                    </div>

                    {/* Right Drift */}
                    <div style={{ padding: 12, background: "#0f172a", borderRadius: 8, border: "1px solid #1e3a5f" }}>
                      <div style={{ fontSize: 11, color: "#7dd3fc", fontWeight: 600, marginBottom: 8, display: "flex", alignItems: "center", gap: 6 }}>
                        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="#7dd3fc" strokeWidth="2"><polygon points="6,22 22,4 22,22" /></svg>
                        Right Snow Drift
                      </div>
                      <div style={{ display: "flex", flexDirection: "column", gap: 8 }}>
                        <CautionInput label="Peak Drift Load" value={snowDriftR_psf} onChange={v => setSnowDriftR_psf(v)} unit="psf" />
                        <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 8 }}>
                          <CautionInput label="Start (tapers from 0)" value={snowDriftRStart} onChange={v => setSnowDriftRStart(v)} unit="ft" />
                          <CautionInput label="End (peak)" value={snowDriftREnd} onChange={v => setSnowDriftREnd(v)} unit="ft" />
                        </div>
                      </div>
                      <div style={{ marginTop: 6, fontSize: 10, color: "#475569" }}>
                        0 at {snowDriftRStart} ft → {snowDriftR_plf.toLocaleString(undefined, { maximumFractionDigits: 1 })} plf peak at {snowDriftREnd} ft
                      </div>
                    </div>
                  </div>
                )}
              </div>

              {/* Additional Uniform Loads */}
              <div style={s.card}>
                <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: 12 }}>
                  <div style={s.cardTitle}>Additional Uniform Loads</div>
                  <button style={s.btn("primary")} onClick={addUL}>+ Add</button>
                </div>
                {uniformLoads2.length === 0 && (
                  <div style={{ fontSize: 12, color: "#475569", fontStyle: "italic" }}>No additional uniform loads. Click + Add to create one.</div>
                )}
                {uniformLoads2.map((ul, i) => (
                  <div key={i} style={{ marginBottom: 12, padding: 10, background: "#0f172a", borderRadius: 8, border: "1px solid #1e293b" }}>
                    <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: 6 }}>
                      <div style={{ fontSize: 11, color: "#f59e0b", fontWeight: 600 }}>Additional Uniform Load {i + 1}</div>
                      <button style={{ ...s.btn("danger"), padding: "3px 10px", fontSize: 11 }} onClick={() => removeUL(i)}>✕</button>
                    </div>
                    <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr 1fr 1fr", gap: 8, alignItems: "end" }}>
                      <div style={{ display: "flex", flexDirection: "column", gap: 2 }}>
                        <label style={{ fontSize: 11, color: "#94a3b8", fontWeight: 500 }}>Load Type</label>
                        <select
                          value={ul.type}
                          onChange={e => updateUL(i, "type", e.target.value)}
                          style={s.select}
                        >
                          <option value="Dead Load">Dead Load</option>
                          <option value="Live Load">Live Load</option>
                        </select>
                      </div>
                      <CautionInput label="Magnitude (plf)" value={ul.w} onChange={v => updateUL(i, "w", v)} unit="plf" />
                      <CautionInput label="Start" value={ul.a} onChange={v => updateUL(i, "a", v)} unit="ft" />
                      <CautionInput label="End" value={ul.b} onChange={v => updateUL(i, "b", v)} unit="ft" />
                    </div>
                  </div>
                ))}
              </div>

              {/* Point Loads */}
              <div style={s.card}>
                <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: 12 }}>
                  <div style={s.cardTitle}>Point Loads</div>
                  <button style={s.btn("primary")} onClick={addPL}>+ Add</button>
                </div>
                {pointLoads2.length === 0 && (
                  <div style={{ fontSize: 12, color: "#475569", fontStyle: "italic" }}>No point loads. Click + Add to create one.</div>
                )}
                {pointLoads2.map((pl, i) => (
                  <div key={i} style={{ marginBottom: 12, padding: 10, background: "#0f172a", borderRadius: 8, border: "1px solid #1e293b" }}>
                    <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: 6 }}>
                      <input
                        type="text"
                        value={pl.name}
                        onChange={e => updatePL(i, "name", e.target.value)}
                        style={{
                          fontSize: 11,
                          color: "#f59e0b",
                          fontWeight: 600,
                          background: "transparent",
                          border: "none",
                          borderBottom: "1px dashed #334155",
                          outline: "none",
                          padding: "2px 0",
                          fontFamily: "inherit",
                          width: "auto",
                          minWidth: 80,
                          maxWidth: 220,
                        }}
                        onFocus={e => e.target.style.borderBottomColor = "#f59e0b"}
                        onBlur={e => {
                          e.target.style.borderBottomColor = "#334155";
                          if (!e.target.value.trim()) updatePL(i, "name", `Point Load ${i + 1}`);
                        }}
                      />
                      <button style={{ ...s.btn("danger"), padding: "3px 10px", fontSize: 11 }} onClick={() => removePL(i)}>✕</button>
                    </div>
                    <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr 1fr", gap: 8, alignItems: "end" }}>
                      <div style={{ display: "flex", flexDirection: "column", gap: 2 }}>
                        <label style={{ fontSize: 11, color: "#94a3b8", fontWeight: 500 }}>Load Type</label>
                        <select
                          value={pl.type}
                          onChange={e => updatePL(i, "type", e.target.value)}
                          style={s.select}
                        >
                          <option value="Dead Load">Dead Load</option>
                          <option value="Live Load">Live Load</option>
                        </select>
                      </div>
                      <CautionInput label="Magnitude (lb)" value={pl.P} onChange={v => updatePL(i, "P", v)} unit="lb" />
                      <CautionInput label="Location" value={pl.d} onChange={v => updatePL(i, "d", v)} unit="ft" />
                    </div>
                  </div>
                ))}
              </div>

              {/* Beam Viz Tab 2 */}
              <div style={s.card}>
                <div style={s.cardTitle}>Loading Model</div>
                <BeamViz
                  span={spanNum}
                  uniformLoads={[
                    ...(roofTotal_plf > 0 ? [{ w: roofTotal_plf, a: 0, b: spanNum }] : []),
                    ...uniformLoads2.filter(u => u.w !== 0 && u.b > u.a),
                  ]}
                  pointLoads={pointLoads2.filter(p => p.P !== 0)}
                  triangularLoads={roofLoadType === "Snow Load" ? [
                    ...(snowDriftL_plf > 0 && snowDriftLEnd > snowDriftLStart ? [{ wStart: snowDriftL_plf, wEnd: 0, a: snowDriftLStart, b: snowDriftLEnd }] : []),
                    ...(snowDriftR_plf > 0 && snowDriftREnd > snowDriftRStart ? [{ wStart: 0, wEnd: snowDriftR_plf, a: snowDriftRStart, b: snowDriftREnd }] : []),
                  ] : []}
                />
              </div>

              {/* Tab 2 Results + Diagrams */}
              {tab2Results && (
                <>
                  <div style={s.card}>
                    <div style={s.cardTitle}>Demand Results</div>
                    <div style={s.grid(3)}>
                      <div style={s.resultBox(comparison && !comparison.moment.pass)}>
                        <div style={s.resultLabel}>Max Moment</div>
                        <div style={{ ...s.resultVal, color: comparison && !comparison.moment.pass ? "#ef4444" : "#38bdf8" }}>{fmt(tab2Results.maxM, 0)}</div>
                        <div style={{ fontSize: 10, color: "#64748b" }}>lb·ft</div>
                      </div>
                      <div style={s.resultBox(comparison && !comparison.shear.pass)}>
                        <div style={s.resultLabel}>Max Shear</div>
                        <div style={{ ...s.resultVal, color: comparison && !comparison.shear.pass ? "#ef4444" : "#38bdf8" }}>{fmt(tab2Results.maxV, 0)}</div>
                        <div style={{ fontSize: 10, color: "#64748b" }}>lb</div>
                      </div>
                      <div style={s.resultBox(comparison && !comparison.deflection.pass)}>
                        <div style={s.resultLabel}>Max Deflection</div>
                        <div style={{ ...s.resultVal, color: comparison && !comparison.deflection.pass ? "#ef4444" : "#38bdf8" }}>{fmt(tab2Results.maxDefl, 3)}</div>
                        <div style={{ fontSize: 10, color: "#64748b" }}>in</div>
                      </div>
                    </div>
                  </div>

                  <div style={s.card}>
                    <div style={s.cardTitle}>Diagrams</div>
                    <Diagram x={tab2Results.x} y={tab2Results.M} title="Moment" unit="lb·ft" color="#3b82f6" fillColor="rgba(59,130,246,0.10)" overlayX={capacityEnvelope ? capacityEnvelope.x : undefined} overlayY={capacityEnvelope ? capacityEnvelope.M : undefined} yTickInterval={5000} />
                    <Diagram x={tab2Results.x} y={tab2Results.V} title="Shear" unit="lb" color="#f59e0b" fillColor="rgba(245,158,11,0.10)" overlayX={capacityEnvelope ? capacityEnvelope.x : undefined} overlayY={capacityEnvelope ? capacityEnvelope.V : undefined} yTickInterval={1000} />
                    <Diagram x={tab2Results.x} y={tab2Results.defl} title="Deflection" unit="in" color="#06b6d4" fillColor="rgba(6,182,212,0.08)" overlayX={tab1Results ? tab1Results.x : undefined} overlayY={tab1Results ? tab1Results.defl : undefined} />
                  </div>
                </>
              )}

              {/* Comparison Table */}
              {comparison && (
                <div style={s.card}>
                  <div style={s.cardTitle}>Capacity vs. Demand Comparison</div>

                  <div style={{ textAlign: "center", marginBottom: 16 }}>
                    {comparison.overall ? (
                      <span style={s.badge(true)}>✓ PASS — Joist Within Capacity</span>
                    ) : (
                      <span style={s.badge(false)}>✕ Joist Needs Reinforcement — Check Graphs Above</span>
                    )}
                  </div>

                  <table style={{ width: "100%", borderCollapse: "collapse", fontSize: 13 }}>
                    <thead>
                      <tr style={{ borderBottom: "2px solid #334155" }}>
                        <th style={{ textAlign: "left", padding: "8px 12px", color: "#94a3b8", fontWeight: 600 }}>Parameter</th>
                        <th style={{ textAlign: "right", padding: "8px 12px", color: "#94a3b8", fontWeight: 600 }}>Existing Capacity</th>
                        <th style={{ textAlign: "right", padding: "8px 12px", color: "#94a3b8", fontWeight: 600 }}>With Added Load</th>
                        <th style={{ textAlign: "right", padding: "8px 12px", color: "#94a3b8", fontWeight: 600 }}>Ratio</th>
                        <th style={{ textAlign: "right", padding: "8px 12px", color: "#94a3b8", fontWeight: 600 }}>Max Deviation</th>
                        <th style={{ textAlign: "center", padding: "8px 12px", color: "#94a3b8", fontWeight: 600 }}>Status</th>
                      </tr>
                    </thead>
                    <tbody>
                      {[
                        { name: "Moment (lb·ft)", ...comparison.moment, isDefl: false },
                        { name: "Shear (lb)", ...comparison.shear, isDefl: false },
                        { name: "Deflection (in)", ...comparison.deflection, isDefl: true },
                      ].map((row, i) => {
                        // When failing, show worst-point values; when passing, show max values
                        const displayDem = !row.pass ? row.worstDemand : row.demand;
                        const displayCap = !row.pass ? row.worstCap : row.cap;
                        return (
                        <tr key={i} style={{ borderBottom: "1px solid #1e293b", background: !row.pass ? "#7f1d1d15" : "transparent" }}>
                          <td style={{ padding: "8px 12px", fontWeight: 500 }}>
                            {row.name}
                            {!row.pass && <div style={{ fontSize: 9, color: "#f87171", fontWeight: 400, marginTop: 2 }}>at worst deviation</div>}
                          </td>
                          <td style={{ padding: "8px 12px", textAlign: "right", fontFamily: "'JetBrains Mono', monospace" }}>
                            {row.isDefl ? fmt(displayCap, 3) : fmt(displayCap, 0)}
                          </td>
                          <td style={{ padding: "8px 12px", textAlign: "right", fontFamily: "'JetBrains Mono', monospace", color: !row.pass ? "#ef4444" : "#e2e8f0", fontWeight: !row.pass ? 700 : 400 }}>
                            {row.isDefl ? fmt(displayDem, 3) : fmt(displayDem, 0)}
                          </td>
                          <td style={{ padding: "8px 12px", textAlign: "right", fontFamily: "'JetBrains Mono', monospace", color: !row.pass ? "#ef4444" : "#94a3b8" }}>
                            {displayCap > 0 ? fmt(displayDem / displayCap * 100, 1) + "%" : "—"}
                          </td>
                          <td style={{ padding: "8px 12px", textAlign: "right", fontFamily: "'JetBrains Mono', monospace", color: !row.pass ? "#ef4444" : "#10b981", fontWeight: !row.pass ? 700 : 400 }}>
                            {row.pass ? "—" : "+" + (row.isDefl ? fmt(row.maxDev, 3) + " in" : fmt(row.maxDev, 0) + (row.name.includes("Moment") ? " lb·ft" : " lb"))}
                          </td>
                          <td style={{ padding: "8px 12px", textAlign: "center" }}>
                            <span style={{ ...s.badge(row.pass), fontSize: 11, padding: "2px 10px" }}>
                              {row.pass ? "PASS" : "FAIL"}
                            </span>
                          </td>
                        </tr>
                        );
                      })}
                    </tbody>
                  </table>
                </div>
              )}
            </>
          )}
        </div>
      )}

      {/* ═══════════ TAB 3 — always mounted, hidden when inactive ═══════════ */}
      <div style={{ display: activeTab === "tab3" ? "block" : "none", background: "#0f172a", minHeight: "calc(100vh - 100px)" }}>
        {!joistRecord ? (
          <div style={s.panel}>
            <div style={{ ...s.card, borderColor: "#f59e0b", color: "#fcd34d" }}>
              Please select a joist and span in Tab 1, and enter additional loads in Tab 2, before running the reinforcement check.
            </div>
          </div>
        ) : (
          <OWSJReinforcementCalc
            span={reinfProps.span}
            depth={reinfProps.depth}
            RE={reinfProps.RE}
            dM={reinfProps.dM}
            Iunreinf={reinfProps.Iunreinf}
            deflUnreinf={reinfProps.deflUnreinf}
            inp={reinfInp}
            setInp={setReinfInp}
            webLocked={reinfWebLocked}
            setWebLocked={setReinfWebLocked}
            chordLocked={reinfChordLocked}
            setChordLocked={setReinfChordLocked}
            scheduleZones={scheduleZones}
          />
        )}
      </div>
    </div>
  );
}
